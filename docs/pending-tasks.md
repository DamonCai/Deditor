# 当前待办：只列剩余工作

更新：2026-09-22。已完成部分移至[完成记录](completed-tasks.md)，本页不再重复列为待办。此前“13 组”混合了实现、优化和验收，不能理解为 13 个功能都没做；按下面的具体缺口判断状态。**待验收表示证据不足，不表示功能未实现或已确认有缺陷。**

## 待实现

| 项目 | 还缺什么 | 完成条件 |
| --- | --- | --- |
| Markdown 格式符空选区自动配对 | 空光标输入 `*`、`_`、反引号、`$`、`~`、`=`、`^` 等格式符时的自动闭合、闭合符跳过及成对退格；与现有 Markdown 输入规则兼容 | 补实现，并核对字面输入、转义、代码/公式、组词及撤销不被破坏。当前代码明确将空选区格式符交给输入规则，见 [inputAssist](../src/lib/markdownVisual/inputAssist.ts) |
| 独立词典与忽略词管理 | 应用自己的词条/忽略词添加、移除和持久化管理 | 完成管理入口、存储与拼写检查联动。范围依据[收尾记录](closeout-2026-09-22.md)，不把操作系统自带词典当成已实现 |

## 待优化与性能补测

| 项目 | 还缺什么 | 完成条件 |
| --- | --- | --- |
| 大文档剩余开销 | 千节/千表真实组件 JSDOM 单轮初始化仍约 10.1 秒，标题更新目录五次中位约 0.9 秒；继续定位冷加载和标题引起的全文目录更新 | 在相同样例上给出前后重复测量，并保持完整内容、保存和撤销一致；现有数据不是原生按键延迟，见[上下文基准](markdown-context-performance-2026-09-22.md) |
| 大规模真实界面性能覆盖 | 100KB/1MB 文档头中尾输入；超过 8 个标签后的冷恢复；大量代码围栏及主题切换；千/万行历史全部展开与导航；大量文件全局搜索的真实磁盘 I/O | 补真实界面的帧延迟/耗时与内容一致性记录，发现问题再修复。具体样例和已有基线见[覆盖矩阵](markdown-performance-coverage-2026-09-22.md) |

## 功能已有实现，剩余验收

| 项目 | 仅剩的操作或场景 | 所需条件 / 依据 |
| --- | --- | --- |
| 链接悬浮卡片 | 从真实悬浮入口进入编辑，确认/取消修改、移除链接、继续输入，再保存/撤销/关闭重开；核对实际源码与焦点 | 本机浏览器及原生应用可推进。组件 API 路径已验收，不能继续列为“编辑功能未实现”；见[完成记录](completed-tasks.md) |
| 真实中文输入法 | Markdown 实际组词、候选选择、组词期间光标/滚动与保存快捷键；XMind 候选窗口定位 | 需要能产生真实候选的原生输入链；粘贴中文和模拟 composition 不替代，见[输入链定位](closeout-shortcuts-2026-09-22.md) |
| Windows 原生 | WebView2 编辑与快捷键、文件面板、系统拖放、多窗口恢复、XMind 保存后由原版读取 | 需要 Windows 环境与原生应用；macOS 通过不覆盖这些场景 |
| macOS 系统桌面交互 | Dock 菜单实际点击新建窗口；Finder 文件/目录拖入目标窗口；多窗口跨 Space 全屏动画与焦点归属 | 需要稳定的 Dock/系统拖动控制；文件菜单及快捷键新建窗口不重复列入，见[窗口验收边界](new-editor-window-2026-09-21.md) |
| 系统拼写菜单 | 开启拼写后实际弹出 macOS 建议菜单，并执行更正/忽略词动作，核对正文及撤销 | 需要真实 Option+右键或有效的系统键盘菜单操作；仅“应用未拦截”不算菜单通过，见[拼写入口记录](closeout-shortcuts-2026-09-22.md) |
| 原生剪贴板与拖动 | 与 Excel/钉钉等应用的富文本复制粘贴往返；跨多屏持续拖选；主实时预览分隔线原生拖动后的尺寸、选区及原文核对 | 需要对应应用和多屏/稳定拖动条件，见[并行收尾边界](closeout-2026-09-22.md) |
| 真实外部服务 | 真实图床/PicGo 上传完整链路、远程 iframe 交互，以及远程 PlantUML 大图成功/超时后的恢复 | 需要测试账户或对应可用服务；离线 SVG、模拟网络不替代，见[性能覆盖](markdown-performance-coverage-2026-09-22.md) |
| XMind 原版视觉 | 紧凑形状、圆角及主题组合的实际截图对照；补无 theme/无填充根的显式白字及更多主题对照，明确自动字色差异 | 需要稳定操作原版 XMind；对照差异不直接当作修复要求，不把可读标题改成不可见，见[原版对照记录](closeout-xmind-2026-09-22.md) |
| 混合文档和多窗口组合 | 同一整合包内 Markdown↔XMind 切换、连续保存/撤销与历史隔离；两个原生窗口之间的近期文件同步和选中文件后的焦点/编辑 | 本机独立包可推进；已有单文档与自动化证据不重复验收，见[近期文件记录](diagram-recent-files-2026-09-22.md) |
| 长期稳定性 | 长时间打开/编辑/关闭文档后的 WebView 内存与资源释放；长文/大图连续缩放、拖动、切表时的资源增长与内容一致性 | 需独立长时测试，记录运行时长、操作量和资源曲线；现有有限次压力结果见[XMind 收尾](closeout-xmind-2026-09-22.md)及[性能检查](markdown-performance-audit-2026-09-22.md) |

## 后续更新规则

每完成一个具体动作或场景，就从本页删除对应文字，并把证据补入完成记录；一行全部闭环才删除整行。发现新缺陷时记录复现条件，不能把“没验过”写成“未实现”。历史文档保留当时事实，最新状态以本页为准。
