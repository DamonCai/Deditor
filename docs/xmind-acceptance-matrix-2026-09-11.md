# XMind 接续验收矩阵

最新增量见[第十轮记录](xmind-round10-verification-2026-09-11.md)：九种边界、五种概要与极坐标/固定端点已实现并补自动回归；浏览器控制点拖动、概要切换及一次撤销通过。86 核心/128 组件通过不是全量验收结论；原生新包与原版读取、候选定位和 Windows 仍未验证。手动联系额外折点及复杂组合继续处理。

基线：`bfe1bc3`，本轮在 `/Users/damon/Documents/ai/Deditor` 接续。历史第五批的 58 项核心与 91 项组件测试已在本机重跑通过；本机 tsx CLI 的 IPC 被沙箱限制，使用 `node --import tsx scripts/test-xmind.ts` 执行同一测试入口。

状态用“历史样例通过 / 已复现 / 修复待复测 / 本轮通过 / 未验证”区分。任何一个样例通过都不代表该类别的所有变体通过。原版输入全部为本轮新建；不使用用户文档、历史或持久化状态。

| 类别 | 需要核对的对象与组合 | 当前证据及状态 |
| --- | --- | --- |
| 常用结构 | 思维导图 1–6 分支、左右逻辑图、上下组织图、括号、左右鱼骨、水平时间轴；不等长子树及折叠 | 历史默认样例通过；本机核心回归通过 |
| 时间轴变体 | 水平穿轴、垂直穿轴、水平离轴、左右方向；空分支、多层明细、长标题、分组、混合结构、折叠 | 已新增垂直穿轴、水平离轴、水平反向实现；原版新建确认字段及主轴/明细方向，生成样例和浏览器复测通过。离轴反向变体尚未在原版确认 |
| 模板与继承 | 无主题、显式样式、inherited、各层级主题、彩虹分支、文字自动颜色、背景、主题覆盖 | 已修复本轮原版主题的 inherited 彩虹填充、level3、20% 填充透明度与自动文字色；红/绿样例和显式覆盖回归通过。第九轮补透明填充/alpha 与画布背景组合的自动黑白字，浏览器和回归通过；更广模板、原版自动颜色规则仍需样例 |
| 主题形状 | 圆角、矩形、椭圆、菱形、下划线、胶囊；原版可选其他形状；文字/标签/图片/图标组合 | 面板继承/未知值已修复；补圆形、双下划线、平行四边形、六边形、圆角六边形、弧边矩形及轮廓求交，几何/浏览器通过。已保存原版形状字段探针；已追加原版采集的 13 项高级、7 项标点、15 项流程图，几何/归档/组件及原版总览比对通过；标签移到轮廓外，标记位于标题旁。紧凑形状折行、部分圆角尺寸仍未声称像素完全一致 |
| 分组 | 边界/概要形状、填充、线型、宽度、标题；嵌套/交叉、拖动、删除成员、撤销 | 补矩形/圆角边界、边界和概要线宽/线型、无填充显示与编辑，保存撤销和浏览器通过。第九轮补范围起止主题编辑、拖动预览/取消/一次撤销和交叉范围/概要后代保留；修复反向及垂直时间轴手柄方向，浏览器和原生显示通过。原生范围面板扩展/撤销通过；其它形状、原版拖动范围及复杂组合仍待核对 |
| 联系 | Cartesian/极坐标、直线/折线/曲线、端点重连、各箭头、与不同形状求交、跨组拖放 | 新增多形状端点求交、inherited 样式解析；400% 联系文字输入框限制在可见区域，末尾输入和完整提交通过。第九轮补联系格式面板、11 个原版箭头字段、端点重连预览/取消及一次撤销，7 种联系默认路径已原版整图校准。极坐标、固定端点与箭头精细视觉仍未全量核对 |
| 多选格式 | 相同值、混合字号/填充/形状/字重、一次修改/撤销、部分父子选中、只读 | 混合值明确显示、批量修改一次撤销恢复各自字号；无填充开关保留每个主题原有颜色/alpha。组件和真实浏览器通过 |
| 文字与 IME | 真实组合输入、Enter/Esc、候选定位、多行、长草稿、10%/400% 缩放、未离焦保存 | 补 keyCode 229 事件隔离、可滚动且不越界的草稿框，移除输入后滚动回顶，联系线不遮挡草稿。第八轮新包联系真实输入也完成 nihao 上屏、组合 Return/Escape 隔离及不离焦 Cmd+S；归档确认“你好shi”。400% 末尾输入/提交、约 10% 12px 可读编辑通过；macOS 真实拼音已验证：逐键 nihao→空格得到“你好”；组合状态 Return 保留编辑，Escape 取消本次拼音且保留草稿；普通 Return 提交。“你好shi是节”已原生另存、重开并被原版读取。应用截图可见带下划线预编辑文本，但候选浮窗未被截图捕获，候选定位仍未计通过 |
| 编辑组合 | 增删、重排、父主题、自由主题、批注、边界/概要、大纲/查找、跨工作表链接、拖放取消、历史恢复 | 历史及本机组件回归通过；复杂新组合继续核对 |
| 外观和输入 | 亮/暗、720px、悬停/选择/禁用/焦点、Tab/Enter/Esc、多标签与工作表切换 | 本轮亮/暗、窄窗口、选中/混合/禁用、工作表切换及极端缩放已浏览器复测。原生字体与候选定位待验证 |
| 文件互通 | 打开/保存/另存/重开、未知字段/附件/工作表保留、原版无修复提示 | 本轮结构样例已被原版打开并另存为 native-shape-probe.xmind；最终源码独立 Review6 包构建及归档核心回归执行中。Review6 已原生打开、未离焦 Cmd+S、另存 native-review6-ime.xmind、关闭重开；原版打开同文件，中文及四张工作表保留且无修复提示。第八轮已重建独立包并冷启动；标签/35 项新增形状均已包含。本批新样例的原生另存及原版读取继续进行，最终全量一致性仍需收口 |
| Windows | WebView2 字体/缩放/真实 IME/快捷键/对话框/拖放/归档互通 | 当前只有 macOS 环境，未验证，不以 jsdom 或 macOS 替代 |
| 收口 | 每项发现闭环、3–5 轮不同用例、完整回归/构建、最终源码与证据一致 | 未完成；正式应用未安装替换 |

本轮原版新建与编辑样例放在 `tests/artifacts/xmind-round6/`，该目录被忽略。`vertical-with-details.xmind` 是原版垂直穿轴、四个主主题、第一主主题两个明细的保存快照；`native-timeline-off-axis.xmind` 是同组内容的水平离轴保存快照。带 `native-timeline-vertical` 名称的文件为原版当前工作文件，后续操作可能改变结构，不能仅按文件名判定内容。

## 本轮实际修复与验证

- `scene.ts`：时间轴变体、原版新主题层级/颜色继承、文字对齐、透明度；未知形状/分组仍保留原字段并提示近似。
- `shapes.ts` / `relationship.ts`：基础形状几何、虚线边框、圆与胶囊等真实轮廓上的联系端点，关系样式中的 inherited 不再覆盖主题颜色。
- `XmindView`：混合值、批量撤销、保留独立填充颜色；边界/概要的线宽线型和已支持形状入口。中英文同步。
- `draft.ts` / Canvas / Relationship：编辑框按可见区域限宽限高、长草稿内部滚动、最小可读字号 12px；只改变草稿显示，不改存档字号。原先每次输入强制滚动到顶部的逻辑已移除。草稿层在联系线之上。

### 五个不同验证维度

轮 1 — 正向用例：四种时间轴、原版新主题继承、多选字号及边界格式修改。
  期望：结构方向正确，颜色可读，格式面板与画布一致。
  实测：垂直明细交替左右，离轴主主题交替上下，反向水平布局镜像；浏览器 24/18 字号批量改 28，再一次撤销恢复 24/18；边界改 5pt/点划线/矩形/无填充即时生效。
  结果：通过已列样例。

轮 2 — 边界用例：12 个基础形状的窄/高/宽内容、全角度端点，空及长 Unicode 草稿、未知形状与 alpha 颜色。
  期望：坐标有限，内容有容纳空间，端点接触轮廓，不因查看或切换填充丢原字段。
  实测：核心几何断言和组件 SVG/存档断言通过；主题形状全图浏览器检查通过。未将这些结果称为全部原版像素一致。
  结果：通过已列样例。

轮 3 — 交互组合：多选混合值→样式→撤销；带边界的长标题/混合结构/折叠；分组修改→保存→撤销；主题/联系文字草稿事件。
  期望：每次操作可独立撤销，成员和未知字段保留，组合输入的 Enter/Esc 不提前提交。
  实测：组件与归档回归通过。isComposing/keyCode 229 为模拟事件，不是实际输入法证据。
  结果：自动回归及 macOS 真实 IME 按键隔离通过；候选浮窗定位尚未取得截图证据。

轮 4 — 真实浏览器：窄窗口、亮暗主题、400% 长草稿末尾输入、约 10% 可读编辑、400% 联系标签。
  期望：输入框始终在画布内；长内容可滚到末尾；普通提交保留全文。
  实测：400% 输入 END 后末尾可见，提交后全文含 END；联系文字同样通过。10% 双行草稿框约 105×42px，scrollHeight/clientHeight 同为 398（世界坐标 CSS 像素），无意外折行；12px 最小显示字号不写入文件。
  结果：通过。截图在本任务工具记录中，未伪称已保存为磁盘 PNG。

轮 5 — 完整回归及构建：核心入口、完整组件回归、TypeScript/Vite、独立 Tauri 测试包。
  期望：既有能力不回退，最终源码可构建。
  实测：结果以 `tests/artifacts/xmind-round6/{core-final,regression-final,native-build-final}.log` 为准，构建完成后记录数字和源码哈希。
  结果：最新核心 66 项、完整组件 110 项通过，Tauri 测试包构建成功；之后新增的形状字段修正仍待最终重跑。

## 复现与剩余阻塞

本轮只使用自己新建的原版/生成样例。可重复生成的入口：

```sh
node --import tsx scripts/create-xmind-round6.ts
node --import tsx scripts/test-xmind.ts
npm run test:regression
npm run tauri -- build --bundles app --config tests/xmind-native-review-round6.conf.json
```

独立浏览器入口：`tests/xmind-review.html?probe=round6`（时间轴）和 `?probe=round6-styles`（形状/分组/联系文字），加 `&dark` 验证暗色。本轮后段用 `/tmp/deditor-xmind-round6-preview` 的源码快照在本机 5174 端口验证，避免其他任务修改共用源码造成 HMR 打断草稿；该快照不是交付源码。

Review6 使用独立 identifier `com.deditor.xmindreview6`，清空文件关联，不读取正式应用状态，不安装覆盖正式应用。构建成功不代表启动或原生验证通过。

桌面先前锁屏，现已恢复；用户协助切换中文输入法后已完成上述真实拼音与原生文件流程。原版扩展形状采集继续进行，存档字段见 native-shape-fields.json（当前高级区 13 项）。原版模板/分组/关系及最终源码冷启动仍继续核对。当前没有 Windows 真机环境，Windows 全流程仍待验证。

不要将本轮通过的样例或日志计数当作整个 XMind 验收已完成；继续矩阵中的原版未验收项。

### 原版形状字段补充

原版基础胶囊实际存档为 `org.xmind.topicShape.ellipserect.compact`；此前 `pill` 在原版显示为矩形。本轮已修正面板写入字段，保留旧 pill 的本地兼容渲染。原版保存证据为 `native-basic-shapes.xmind`。高级区 13 项通过原版菜单逐项保存采集，工作文件为 `native-shape-catalog.xmind`，采集 JSON 按项保存根主题。采集证明字段身份，不等于对应渲染已验收。

最新回归与包：第九轮核心 81、组件 124 项通过，最终方向修正包冷启动及原生上下手柄显示已检查；详细证据和仍受阻项见 [第九轮记录](xmind-round9-verification-2026-09-11.md)。这些进展不代表矩阵全部完成。

透明填充后续：核心 81、组件 124 项通过，颜色包已构建并启动；源文件/二进制清单为 round9/build-colors-verification.json。重置 CUA 后原生连接恢复：颜色两表显示、未离焦保存及重开通过；边界真实拖动扩展到图片、一次撤销、重做保存和重开通过。归档分别只有标题和范围变化，其余内容保留，证据为 round9/native-colors-groups-verification.json。原版读取过程中再次锁屏，尚未取得读取结果；概要和联系真实拖动等仍继续待验收。没有把全量范围标为完成。

继续修正：双侧范围落点不再误选另一侧，左右手柄分别计算顺序；浏览器左右扩展和一次撤销通过。概要范围跨入带子边界分支通过浏览器实测。联系控件固定屏幕尺寸，选中联系置于其它联系之上；9% 缩放下连续重连共享终点及逐步撤销通过。最新核心 82 项；固定源码快照的回归、构建及原生未验收状态见第九轮记录。Mac 仍锁屏时不能补原版字段/形状采集、候选定位或新包原生互通，Windows 也仍未验证。
