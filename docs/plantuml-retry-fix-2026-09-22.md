# PlantUML 超时后同节点重试修复

在真实产品 hydrate 模块中复现：请求超时后，原占位仍保留 `data-plantuml-hydrated=1`，再次调用 hydrate 会跳过该节点。新增回归修前失败（请求数期望2、实际1），修复在失败后清除已加载标记，成功后移除旧 error 样式；失败时仍保留原始图源码。

`npm run test:plantuml-retry` 使用 JSDOM 与受控 fetch，等待产品真实 5 秒计时器触发 abort，再对同一节点重试；验证源码保留、第二次请求、SVG 恢复、错误样式清理和成功后不重复请求。修后通过，已接入 test:all。TypeScript 构建检查通过；未跑完整全量或打原生包。

这是可复现状态缺陷的代码修复，不是真实远程服务超时验收；EXT-04 的远程超时与实际界面恢复仍保留。没有增加新限制或改变超时长度。
