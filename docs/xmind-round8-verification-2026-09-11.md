# XMind 第八轮：标点、流程图与联系输入

整体接续任务仍在进行。所有原版输入及保存均为本任务生成的样例，不读取用户正式文档或正式应用状态。

## 本轮实现与已验证证据

- 原版形状面板逐项选取并保存 7 项标点、15 项流程图；字段固定到 `tests/fixtures/xmind-native-{punctuation,flowchart}-shapes.json`。采集过程先确认选择生效再保存；归档采集脚本只读自建 `native-shape-catalog.xmind`。
- 标点保留开放轮廓，形状切换时按原版关闭填充并保留原填充颜色；引号采用实心引号。按原版同文件总览调整不同括号的左右留白。
- 流程图加入判断、手动输入、预定义过程、手动操作、数据库、文档、多文档、延迟、数据、页内/页外连接、双圆、左尖标签及两种梯形。数据库顶部、多文档后层和预定义过程内部线独立绘制；内容安全区域与轮廓求交共享几何。页内/页外连接为上方符号、下方文字。
- `native-palette.xmind` 两张工作表分别包含上述形状、中文/English 两行、优先级、标签。原版打开无修复提示。原版与浏览器总览已比对；原版 `flag` 及页连接的自动折行仍明显更窄，当前未声称像素完全一致。
- 核心 73 项通过；新增组件检查后完整回归 118 项、0 失败。JS DOM 对 SVG `:scope` 选择存在差异，验证改为检查真实直属元素；不以该选择器差异改产品代码。TypeScript 及 Vite/Tauri 独立 Review6 包构建成功。
- 新包冷启动恢复第七轮文件；联系文字的真实 IME 经清空草稿重新验证：逐键 `nihao` + 空格得到“你好”；`shi` + Return 为“你好shi”且仍编辑；再输入 `shi` + Escape 保留“你好shi”且仍编辑。Cmd+S 直接保存已通过磁盘归档确认，其他联系及两个工作表、SVG 附件完整。
- 首次联系输入混入未清除的输入法状态，不能算通过；只采用清空后重新执行的上述结果。应用窗口截图未包含系统候选浮窗，候选定位仍未声称通过。

## 继续检查

联系输入已原生另存为 `native-review6-relationship-ime.xmind`、关闭重开，并由原版打开确认“你好shi”和两张工作表无修复提示，SVG 附件哈希与原样例一致。本批形状面板的原生编辑保存继续验证。联系端点重连、箭头及极坐标、更多分组形状/交叉范围、广泛模板继承继续处理；Windows 原生环境仍不可用。最终源码与交付包一致性须在全部修改结束后再次确认，当前没有替换正式安装。
