# UI 样式统一验证（2026-09-09）

本次统一：Button 的 CSS 状态颜色、输入框与焦点、36px 文件工具栏、设置/阅读分段控件、弹窗按钮、关闭图标、错误提示，以及主题化遮罩/阴影/语义色。未修改 XMind 专用组件和 CSS；共享 Button 的影响纳入相邻回归。

标准入口为 `AGENTS.md` 的“UI 开发规范与统一标准”，`CLAUDE.md` 引用该节。

## 分轮结果

| 轮次 | 用例 | 期望 | 实测 | 结果 |
| --- | --- | --- | --- | --- |
| 轮 1 | HTML 阅读、格式化/保存主路径及工具栏布局 | 正常渲染保存，同类工具栏等高 | 回归 6/6；浏览器实测 Markdown/HTML/JSON/SQL 均为 36px（含 1px 边框） | 通过 |
| 轮 2 | 空内容、中文、Windows 路径、最小窗口和长错误 | 边界输入不丢失，布局和关闭按钮可用 | 回归 8/8；720px 设置面板宽 640px、无越界。720×480 长错误正文高度 238px、scrollHeight 1600px，关闭按钮底边 y=399.25px；查找栏自动换行，替换按钮完整显示 | 通过 |
| 轮 3 | 多标签、视图切换、异步保存及撤销 | 原有操作和编辑记录保持正确 | 回归 10/10；设置和阅读分段控件均为 24px 高、5px 圆角 | 通过 |
| 轮 4 | 格式化失败、连续错误、失败后普通确认 | 原内容不变，错误排队，后续确认清除错误外观 | 回归 9/9；错误弹窗 Close/Esc 正常，下一条普通确认不残留错误 tone | 通过 |
| 轮 5 | 焦点恢复、键盘操作、亮暗主题和邻接功能 | 键盘反馈一致，弹窗关闭后回到触发控件 | 回归 8/8；Tab 的 2px 蓝色轮廓、Enter 关闭、Esc 返回焦点实测正常 | 通过 |

最终复核：`npm run test:regression` **41/41 通过，0 失败**。历史中间结果曾为 39/40，失败项是 XMind 多行标题 inspector 测试；XMind 独立改造更新后，该用例及新增的标签卸载草稿用例均已通过。本次没有修改 XMind 实现或放宽其断言。

`npm run build` 最终通过（TypeScript + Vite）；`git diff --check` 通过。

## 浏览器实测补充

- 普通输入框：32px 高 / 13px 字号；紧凑输入框：24px / 12px；禁用透明度 0.5。
- Ghost 按钮 normal 背景透明，鼠标悬停后为 rgba(0,0,0,0.04)，验证此前被行内背景覆盖的问题已修复。
- 输入框焦点边框为 rgb(53,116,240)，外环为同色 1px。
- Prompt 和错误确认的操作按钮均为 12px 字号、5px 12px 内边距。
- 暗色错误文字为 rgb(255,142,150)，弹窗遮罩为 rgba(0,0,0,0.55)；亮暗主题均完成视觉检查。
- 720px 窗口下 Markdown 次要工具进入“更多”，模式切换按钮仍可见。

浏览器验证使用真实 React 控件与应用 CSS，IPC 为测试替身；不代表 macOS/Windows 安装包验证。临时页面已移除，浏览器尺寸已恢复。

## 最终逐项复核

| 原检查类别 | 完成情况 |
| --- | --- |
| 按钮状态 | 共用 CSS 已接管状态；补齐 ghost / secondary 选中后的 hover / active，悬停仍保留选中底色 |
| 输入框与焦点 | 普通 / 紧凑规格统一；补齐 CodeMirror 查找输入框 24px、12px 字号、3px 8px 留白及焦点外环 |
| 文件工具栏 | Markdown / HTML / JSON / SQL 均为 36px |
| 分段控件 | 设置与阅读模式共用 SegmentedControl，24px 高、5px 圆角 |
| 弹窗操作按钮 | Prompt / Confirm 复用 md 规格；CodeMirror 自带查找按钮适配 sm 的 11px 字号、3px 10px 留白及 2px 键盘焦点 |
| 图标控件 | 应用关闭按钮使用 FiX；补齐全局搜索展开按钮，使用 24px Button + 14px Chevron，搜索 / 替换输入框左侧对齐 |
| 错误提示 | 原生 alert 已替换；验证错误排队、恢复普通确认外观、原内容保留及焦点恢复 |
| 遮罩、阴影与语义色 | 使用主题 token；补齐 Markdown 更多浮层的 popup 阴影，暗色实测 0 6px 16px rgba(0,0,0,0.40) |

最终浏览器检查额外发现并修复了窄窗口 CodeMirror 查找栏不换行、右侧替换按钮被裁切的问题。720×480 下改为自动换行，查找 / 替换按钮均位于面板范围内，并为关闭按钮预留空间。全局搜索面板左右边界为 18px / 702px，搜索和替换输入框左侧均为 63px、高度均为 32px；Aa 的 aria-pressed 与蓝色选中外观一致。Markdown 更多浮层左右边界为 24px / 696px，未越界。

本轮补漏后重新执行完整回归和生产构建，均通过。Vite 仍提示大于 500KB 的 chunk；没有构建错误。开发规范已同步补充到 AGENTS.md，CLAUDE.md 保留统一入口引用。
