# 多 agent 收尾（2026-09-22）

用户要求快速、高质量并行收尾。三个 agent 分别处理原生快捷键、XMind、新窗口；主 agent 处理配对设置、交叉审查和整合。原生桌面/系统剪贴板按槽位串行，浏览器和组件测试并行。开始时 HEAD 为 `3db81f3`，工作区干净。本次不提交、不推送、不替换日常安装应用。

## 实际结果

| 项目 | 结果 |
| --- | --- |
| macOS 正常退出漏恢复内容 | 真实复现已显示的最后字符在 Cmd+Q 后丢失。AppKit 原生 terminate 绕过可取消的 Tauri ExitRequested；增加 `applicationShouldTerminate:` 接入等待写盘协议后，同动作及连续 e/f/g 立即退出均精确恢复 |
| 窗口关闭/取消竞态 | 修复取消后不重新排定恢复写盘、旧关闭确认误确认快速重试、慢写盘期间新增输入遗漏、卸载后旧确认、重复确认造成编辑锁残留；已确认窗口等待其他窗口时锁定交互，取消恢复 |
| 关闭后的异步编辑 | 真实延迟粘贴在最终快照后仍改正文，现对剪切/粘贴、图片上传/粘贴及整理的异步写回加关闭检查；取消关闭后可重新操作；正常非关闭错误仍反馈 |
| 内嵌源码 Cmd+S/Z 字母化 | 同一 WKWebView 的纯 textarea 也复现，可信 composition 事件先于组合键keydown；ABC下普通代码/Mermaid/主源码保存撤销重开通过。定位到本机自动化与拼音输入链，未以拦截中文组词来猜测性修复 |
| XMind 透明根分支穿字 | 透明/半透明根分支改从轮廓侧边起画，保留不透明主题曲线；默认文字颜色未凭猜测调整 |
| 阅读编辑配对偏好 | 新增括号、引号、选区包裹三个独立设置，默认兼容旧行为，总开关继续优先，中英与持久化同步 |
| 系统拼写菜单入口 | 保留普通中英右键菜单，开启系统拼写时用 Alt/Option 右键放行正文/表格的系统菜单，源码维持原有菜单；没有增加独立词典服务 |

详细证据：[原生快捷键/异步编辑](closeout-shortcuts-2026-09-22.md)、[新窗口](closeout-windows-2026-09-22.md)、[XMind](closeout-xmind-2026-09-22.md)、[写作与长文](closeout-writing-2026-09-22.md)。

## 验证

- 整合 `npm run test:all` 退出码0，含147通用/8分隔线、119 XMind核心、107阅读集成及各功能组。运行期间最后增加的异步关闭保护另过最终107阅读集成、7异步关闭、图片剪贴板、23输入剪贴板及10窗口生命周期；新增XMind收尾专项独立执行通过。两项新专项已接入以后 `test:all`。
- XMind 专项：280分支几何、96形状/主题/字号/宽度组合、500混合编辑/归档重开、51历史归档；浏览器千节点50次编辑/50撤销/50重做，亮暗宽窄均有记录。几何与字段通过不等于原版逐像素一致或长期内存通过。
- 写作界面：中英亮暗、720/1280px、键盘/禁用状态；100节34,641字符长文在五处编辑→切标签→撤销，原文精确相同；四次模式往返与保存快照一致。
- 最终生产构建通过，保留已有大chunk提示。最终隔离原生包为 `src-tauri/target/debug/bundle/macos/DEditor Final Closeout.app`，identifier `com.deditor.final-closeout-20260922`，不读取日常应用状态。
- 原生多窗口批次已验证最后字符恢复、第二窗口独立输入、两快照整体退出/恢复/分别保存；该批快照早于最终异步guard提取，不能自动算作最终新包全部交互已通过。最后整合包核验另记下节。
- 最后相关复测另含25图表组及3个Rust窗口协议测试，全部通过。

## 最终整合包原生核验

最终包二进制 SHA-256 为 `c1557556985ea6f0ea2f0672205cb2f0179ce3bd7d4bd817c4be0eb7c06852ae`。验收后重新核对源码清单，无差异。

- 仅打开自建 `/tmp/deditor-closeout-integrated/review.md`，含正文、表格、普通代码和Mermaid。阅读模式显示正常，三个独立配对偏好均显示且默认为开启。
- ABC输入源下逐键输入 ` native-final` 后Cmd+S，文件229字节精确相符；Cmd+Z后保存恢复216字节原始内容；重做保存、关闭并重开后正文完整，文件仍精确一致。
- 重开后输入末尾字符 `q`，立即Cmd+Q。重新启动最终包，恢复的脏文档包含该字符；保存后230字节与预期完全相同。这直接覆盖最终guard提取后的整合包，不借用前一包证据。
- 开启系统拼写，选择错词后发送Alt+Shift+F10，未观察到原生菜单；本次工具没有带修饰键的原生右键接口。因此系统拼写菜单只记录浏览器放行逻辑通过，原生菜单操作仍未验收。
- 测试文档保存关闭后退出隔离应用，恢复状态不再包含测试文档；输入源已读回确认恢复 `com.apple.inputmethod.SCIM.ITABC`。原版XMind和各隔离测试应用均已退出，临时浏览器页与服务已关闭，未替换日常应用。

结果另存 `tests/artifacts/closeout-20260922/native-final-results.json`。

## 仍需区分的边界

真实中文候选选词/定位、Windows 原生和真实图床账户仍缺环境或有效实测；模拟composition、直接中文字符串、模拟网络不能替代。Dock连接超时，实际Dock菜单点击与外部系统拖放未通过；辅助窗口全屏有实测，完整跨窗口/跨Space动画仍未闭环。长时间WebView内存、持续多屏拖选和跨应用富文本往返仍不能由本次有限压力测试推断全面通过。

XMind原版单工作表对照确认无theme、无显式字色的根标题不可见，显式黑色后完整可见，六份文件前后哈希一致；这是原版显示差异，未按猜测改变DEditor的可读默认色。紧凑形状的原版逐像素核对仍因原生打开工具失败而未完成。

完整Markdown格式符空选区自动配对、独立词典/忽略词管理仍是原固定清单的功能边界。此次补齐现有配对行为的独立偏好和系统拼写入口，不声称与Typora所有高级功能等价。

## 可复核证据

`tests/artifacts/closeout-20260922/` 保存本次日志与最终241个源码/配置文件的SHA-256清单。该目录被Git忽略，跨机需单独迁移；各文档提供了可重建样例与命令。原生二进制SHA-256见其中`source-hashes.json`。窗口、XMind和快捷键各自原生批次有独立记录，不能混用包版本。
