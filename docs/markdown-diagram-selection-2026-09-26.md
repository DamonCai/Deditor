# 图块与相邻正文点击选区核查（2026-09-26）

样例：`/Users/damon/Documents/workspace/ai/tmp/问题.md` 中的 Mermaid 图和其下方中文段落。用户补充说明上下交替点击几次后几乎必现，并提供截图：图块已有蓝色选中边框，下方整段却出现蓝色原生选区。先前只凭状态栏和少量截图判断已解决，验收不足。

## 修改

- 阅读编辑的代码/图块预览在捕获阶段处理左键按下。嵌套 SVG 或 `foreignObject` 元素即使截断事件冒泡，图块也能先阻止浏览器建立原生文字选区，再沿用原有的整块选中或源码打开动作。
- 仅在可编辑的阅读视图中，渲染的图块预览禁止原生文字选择；图块仍可由编辑器整块选中，链接和媒体控件保留各自点击行为。只读内容仍可选择文字。
- 修正阅读编辑的隐藏选区样式优先级。ProseMirror 的 `NodeSelection` / `CellSelection` 会保留 DOM 选区供键盘和剪贴板使用，且标记 `.ProseMirror-hideselection`；Milkdown 的 `.milkdown .ProseMirror *::selection` 优先级更高，曾把这段本应隐藏的选区画成蓝色，包括图块之后的整个段落。现将原先仅保护表格的规则扩展到所有隐藏选区；普通正文选区仍显示。
- 实时预览使用独立的预览 DOM，不走这段节点视图鼠标处理；实测普通左键点图、点相邻段落没有残留选区，因此没有改它的选择语义。

## 横向检查与验证

- `test:markdown-diagram-modes` 30 组通过，包括 Mermaid/PlantUML 及别名、阅读/编辑/分栏、普通代码、公式、拖动与新加的嵌套元素截断冒泡用例；链接和相邻正文仍保留默认点击。
- `test:markdown-special-blocks` 8 组、`test:markdown-visual:integration` 108 项与生产构建通过。覆盖保留 HTML、媒体控件、表格/历史/模式切换等相邻交互；没有发现需要同步改动的同类路径。
- Chromium 用完整样例连续四轮“图→下方正文”：图仅整块选中，正文点击后图块选中消失，原生选区为空。实时预览点图和正文后原生选区均为空。
- 独立 macOS WebKit 包使用 `bundle.fileAssociations: []`。实际点击图中 SVG 文字时状态栏为整块 253 字符选中，下方段落无高亮；点击段落后只留下正文光标。实时预览点图和下方文字无可见串选。自建样例已保存并关闭，测试应用退出；未替换日常 DEditor。

## 用户反馈后的连续交替点击

用户说明问题在上下交替点击几次后几乎必现。重新启动同一隔离 WebKit 包，打开自建的原文样例：

- 阅读编辑：预览区域与下方正文连续 30 轮“图→正文”，每轮状态栏分别是 `Ln 18, Col 1 (253 selected)` 与 `Ln 18, Col 77`；两种状态的截图分别只显示图块边框与正文光标，未见交叉高亮。
- 阅读编辑：进一步以图内文字可访问元素为目标，连续 5 轮“图中文字→下方正文”，状态同上；另一次 20 轮操作在中途因系统录屏流错误中断，不计为完整通过。
- 实时预览：右侧图中文字与下方预览段落连续 10 轮交替点击，截图未见跨区高亮；左侧源码光标保持原位，预览区点击本来就不移动源码光标。

这些结果证明当前隔离包在所测点击位置没有再出现报告现象；日常安装包未替换，不能把用户在旧包中的复现当成新包结果。

## 截图反馈后的修正与原生复验

- 截图明确显示“图块蓝框 + 下方段落整片蓝底”，不是状态栏选区位置的问题。检查依赖 CSS 后确认 Milkdown 的选区着色规则覆盖了 ProseMirror 的隐藏选区规则；此前表格已经针对同一冲突加过局部修正。
- 将局部修正推广到阅读编辑的全部隐藏选区后，生产构建、30 组图表模式、34 项表格操作通过；用相同 `bundle.fileAssociations: []` 覆盖配置重打隔离 macOS 包。
- 新包中先在下方正文用 Shift+Left 选出一个字符，截图可见正常蓝色文字选区；再点击图内文字，图块出现蓝框，下方正文恢复普通背景。接着图中文字与下方正文交替 10 轮，状态栏逐轮在图块 253 字符选中和正文光标间切换；末轮再选图，截图未见用户报告的整段蓝底。
- 实时预览仍是独立预览 DOM，不使用 ProseMirror 的隐藏选区规则；此前原生 10 轮交替点击未见串选。本次 CSS 修正仅限阅读编辑，不改变它。

以上是针对用户截图症状的修复包复验；未替换日常安装的 DEditor。

用户随后确认第二张仍显示图中文字和下方段落蓝色连选的截图来自**日常 DEditor**，不是隔离修复包，因此它反映的是尚未更新的版本。已将隔离包重新打开到原文的独立副本，供用户在相同图文位置直接复测；日常应用中的未保存文档未操作。

未把上述点击结果扩展为长时间随机点击、所有 Mermaid 图型、Windows 或触控输入的结论。

## 相邻操作与实时预览追加横查

用户要求继续排查其他操作、实时预览，并提交本次修复。用隔离 macOS 包打开自建样例 `tests/artifacts/diagram-selection-2026-09-26/selection-cross-audit.md`（位于忽略目录，不进入产品），依次放入 Mermaid、普通代码、块公式、表格、SVG 图片，每块上下均有可编辑段落。

- 阅读编辑中，逐一点击图中文字、代码文字、公式、表格单元格、图片，再点击下方段落。图块显示 34 字符整块选中后，段落点击回到正文光标；图片显示 1 个节点选中后也回到正文光标。代码、公式、表格点击后再点段落，没有留下跨块高亮。图块、图片和段落末态均有原生截图复查。
- 先前同一修复包中，Mermaid 图中文字与下方正文交替 10 轮；本轮在实时预览的右侧图中文字与段落又交替 8 轮，末态截图无蓝色连选。实时预览另逐项点击 Mermaid、普通代码、块公式、表格及相邻段落，截图未见串选；左侧源码焦点与内容不变。实时预览是独立非编辑 DOM，普通文字选择仍由浏览器处理，本轮没有发现需要改动的同类路径。
- `test:markdown-diagram-modes` 30 组、阅读集成 108 项、表格 34 项、边界/选区/鼠标/恢复专项、预览限制 4 组、特殊块 8 组和生产构建通过。表格及集成测试的首次并行运行完成；为收集简短日志再次运行，测试本身分别显示 34 项和 108 项通过，外层收尾命令误用了 zsh 只读变量 `status` 而返回 1，不属于测试失败。

日常 DEditor 中仍有用户未保存的文档，未退出、覆盖或安装本次修复包。隔离包使用 `bundle.fileAssociations: []`，用户原始 `问题.md` 未改。上述原生点击由可访问元素触发，不能替代用户的物理鼠标长期操作。

横查后重新执行生产构建，并用相同隔离配置重建 `DEditor Diagram Selection Review.app` 成功；测试文档关闭，隔离应用退出。更新仅提交产品代码、回归与本记录，不提交忽略目录中的原生样例和日志。
