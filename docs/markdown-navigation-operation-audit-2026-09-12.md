# 阅读导航、列表与引用补充验收（2026-09-12）

范围固定为操作清单 A03/A05/A06 的基本导航，以及 E02/E03/E06 的列表和引用键盘操作。测试均使用自建样例；不涉及原生应用、用户文件、提交或推送。

## 确定复现与修复

E03 首列表项无法再缩进时，上游列表命令返回未处理，通用 Tab 缩进随后向正文插入四个空格。真实浏览器使用 `first` 首项，定位末尾后 Tab、输入 X，修复前得到 `first    X`；修复后得到 `firstX`，单独 Tab 原文完全不变。

新增 `src/lib/markdownVisual/listKeys.ts`，优先处理列表 Tab/ShiftTab：保留上游嵌套与提升行为，无法进一步缩进时消费 Tab。表格内列表、代码以及组词过程不由此处理；首项 ShiftTab 原有退出列表行为保留。组件仅新增导入及插件注册两处，不修改样式。

## 自动化

`scripts/test-markdown-navigation-audit.mjs` 在工作区及固定副本 `/tmp/deditor-navigation-audit-x7rz35wy` 各通过 7 组：

1. 第二项 Tab 嵌套、ShiftTab 提升，保存/撤销/重做/重挂载。
2. 首项 Tab 原文不变，多项选区一起嵌套。
3. 嵌套空项 Enter 先提升、再退出，退出后输入与保存/历史。
4. 引用 Enter 续段、再次 Enter 退出；引用起点 Backspace 合并前段，保存/历史。
5. 嵌套引用起点 Backspace 合并前段且后方引用定义精确保留。
6. 根列表首项 ShiftTab 提升；已完成任务首项 Tab 不插空格、不改勾选状态。
7. 表格单元格内列表 Tab/ShiftTab 仍跨格，反向进入前格末列表段，原文不变。

曾出现重挂载后测试选区被初始化恢复到文首：原测试在同一 React act 内先等待、后提交挂载，等待并未覆盖初始化。分开提交挂载与等待后通过；此为测试前置问题，不计作产品缺陷。

## 真实浏览器

自建页 `tests/markdown-navigation-audit-review.html`，独立固定副本服务 5197，避免工作区并行热更新影响样例。

- A03：连续上下经过标题、正文、根/嵌套列表、引用及末段；未出现此前回跳至行内源码现象。嵌套列表行首左移进入前项末尾，行尾右移进入后项起点。
- A05：格式列表内 Home 到 `alpha ` 起点、End 到 ` gamma` 末尾。此为浏览器键位，不替代 macOS/Windows 全套快捷键。
- A06：左右穿过粗体与行内代码边界，源码展开/收起，全文保持一致。
- E03：首项 Tab 修复前后对照；第三项 Tab 嵌入前项子列表，ShiftTab 返回根层。
- E02：嵌套末项连续三次 Enter 后输入 `exit text`，新正文位于列表之外，原有后项保留。
- E06：引用末段连续两次 Enter 后输入 `outside`，成为独立正文，前后内容保留。

引用起点 Backspace 的浏览器动作不计通过：旧固定副本中工具 `selectText(alpha, cursor_before)` 后读取选区仍停在前段末尾，退格实际作用在前段。该操作在本专项自动化已验；没有据此猜测新增产品修复。此固定副本不包含主任务随后完成的共享文本节点源码映射修复。

本记录只覆盖所列链路，不表示 A/E 所有操作组合或原生 IME、Windows、真实拖拽全部通过。最终整合及原生保存由主任务独立核对。
