# XMind 跨机器接续上下文

最新状态（第十六轮）：新增向上穿轴及双向对称垂直时间轴，固定快照 104 核心 / 140 组件、构建通过。Review18 三种结构已完成原生编辑、保存、折叠、关闭重开，归档仅预期字段变化；保存结果原版重读仍待完成。用户要求验证后立即退出应用，现已退出 Review18 与 XMind；原版存在批量恢复旧测试窗口问题，后续须逐份关闭避免占用。见[第十六轮记录](xmind-round16-verification-2026-09-12.md)，整体仍未完成。


历史状态（第十五轮，继续推进第十六轮）：Review17 固定快照完整回归通过（102 核心 / 140 组件），构建、原生冷启动及联系编辑辅助访问通过。多行键盘编辑、反向离轴分组范围修改均完成原生保存/重开和原版读取；最终紫色及粗线箭头已闭环。真实候选窗口、原生整段拖动、向上/对称垂直时间轴及 Windows 等剩余项继续处理。详见[第十五轮记录](xmind-round15-verification-2026-09-12.md)，整体仍未完成。

历史状态（第十四轮，继续进行）：已补向左离轴时间轴，浏览器反向边界拖动/撤销及原生混合结构折叠/保存/重开通过。Review14 完整回归仍为 100 核心 / 139 组件，构建和冷启动通过。单表粗线多行保存已被原版读取且保留空行；原版整图细节、反向结构、紫色最终版及既有原生/Windows 未验收项继续推进。详见[第十四轮记录](xmind-round14-verification-2026-09-12.md)，整体仍未完成。

历史状态（第十三轮，2026-09-12，继续进行）：修正箭头几何、联系多行输入/显示，以及 ZIP 时间戳导致撤销回已保存内容仍不同字节的问题。最终 Review13 完整测试通过（100 核心 / 139 组件），原生多行输入、未离焦保存、延时后撤销回保存状态、关闭重开已通过。原版最终紫色/多行和粗线箭头细节、真实拖动/候选、复杂组合及 Windows 仍未全部验收。详见[第十三轮记录](xmind-round13-verification-2026-09-12.md)。这不是任务完成，继续剩余范围。

历史状态（第十二轮）：整体仍未完成。Review10主题字号、Review11边界字号/加粗与联系字号已完成原生保存、重开及改色前原版读取。原生系统取色器的改色/撤销/重做/保存/重开也通过；最终紫色版本原版重读仍受文件对话框禁用阻塞。新修正部分主题缺省边框和辅助点击/键盘选择，99核心、137组件、完整测试和原生构建通过，Review11已冷启动。直角整段原生拖动仍返回窗口不可用；真实候选定位、更广组合及Windows未全部验收。详见[第十二轮验证](xmind-round12-verification-2026-09-11.md)，不能将批次通过计为整体完成。


历史状态：整体仍未完成。Review7 的有效分组范围、极坐标和手动折点已完成原生保存、关闭重开及原版读取；整体边界也已完成创建、改名、保存、重开及原版 80% 全图读取，嵌套分支与独立主题新边界均正确。

后续已修正缺省文字色、主题 alpha、边界透明度、联系文字颜色，补手动折点新增/删除和直角整段拖动，长路径耗时已优化。继续发现并修正智能主题色板规则和真实逐键字号输入问题，补边界文字格式入口。最新固定快照的完整测试（98 核心 / 136 组件）、macOS 构建及对应浏览器操作通过；Review10 新包启动时仍明确报锁屏，冷启动未计通过；原版新样例读取时工具明确报告 Mac 锁屏，最新原生交互、候选浮窗、Windows 等仍未通过。第十轮互通记录及[第十一轮验证](xmind-round11-verification-2026-09-11.md)区分源码、测试包和实测证据，整体继续未完成。

更新时间：2026-09-11。状态：**整体工作未完成，按用户要求暂停以便换机器**。

后续接续进展以[验收矩阵](xmind-acceptance-matrix-2026-09-11.md)和[第九轮记录](xmind-round9-verification-2026-09-11.md)为准，上述换机暂停及下方第五批为历史状态。第九轮继续修复双侧分组落点/手柄方向、低倍率联系控件及共享终点遮挡；固定源码快照的核心 82 项、组件 125 项和独立 Review6 构建通过，清单为 `tests/artifacts/xmind-round9/build-final-snapshot-verification.json`。颜色/分组旧包原生保存重开已补齐，最新修正仅完成浏览器及自动回归；新包冷启动和原版互通尚未通过，当前工具明确报告 Mac 锁屏。用户已被请求解锁。解锁后先恢复原版 groups.xmind/colors.xmind 读取，再冷启动最新包继续新修正、原版控制点/分组形状、候选定位等剩余项；Windows 仍未验证。

## 用户目标与停止原因

用户要求对照原版 XMind 的功能和展示，一点点验证重构实现，重点是 UI、前端交互和细节缺陷；需要实际修复并完成验证，不能只列差异。

此前助手多次在完成一批修复、单测和原生样例后结束回复，虽然报告中仍列着未验收项。原因是把“批次检查点”当成“整个任务的结束点”，没有立即选择下一项继续执行。这是助手的执行判断错误，不是用户授权不足。早期确实遇到锁屏；用户已解锁，最近几次停止没有由环境阻塞导致。不能把工具偶发失败或测试通过当成再次停工的理由。

接手后直接推进下方未完成清单。每项按“原版复现 → 当前实现复现 → 修复 → 自动回归 → 真实 UI 复测 → 保存互通（相关时）”闭环。一个批次通过后继续下一项，不请求重复的继续授权。用户询问状态时答复后继续工作。只有用户明确暂停/交接、范围全部验收完成或没有任何可推进工作的真实阻塞才结束执行；不得擅自把复杂模板等排除出范围。

## 仓库与迁移

- 本机仓库：`/Users/damon/Documents/workspace/ai/Deditor`。新机器使用自己的仓库根目录，不硬编码这个路径。
- 当前分支：`feature/0.9.0`；交接前 HEAD：`edaf4c5c7424e97ed97f8298fad9e7dc5b3067b7`。以新机器实际 Git 状态核对，保留所有既有改动。
- 所有本任务修复还在工作区，**没有提交或推送**。只 clone/pull 不会拿到修复。
- 新文件 `src/lib/xmind/drop.ts` 原先未跟踪；本次增加的交接说明和测试包配置也需要一起迁移。
- `tests/artifacts/` 被 `.gitignore` 排除，截图、生成归档、日志和 `verification.json` 不会自动通过 Git 同步。
- 本次交接包：`tests/artifacts/xmind-handoff-2026-09-11.zip`。它包含当前修改文件快照、已跟踪改动的 `changes.patch`、Git 基线与文件哈希清单，以及本连续任务已生成的第一至第五批相关验证证据。它不包含完整仓库、依赖、编译产物或用户应用状态。
- 在新机器先获得同一仓库/基线，再把交接包解压到临时目录，核对 `HANDOFF-MANIFEST.json`。`workspace/` 下的路径相对仓库根目录。对干净的同基线检出可恢复这些文件；若新机器也有修改，应比较并合并，不盲目覆盖，也不要同时覆盖文件后再重复应用补丁。`changes.patch` 不包含未跟踪文件；文件快照包含它们。
- 将需要的 `workspace/tests/artifacts/` 一并带过去用于查看历史证据。新一轮功能验证仍生成新样例，不能把历史截图当成新机器或新代码的实测结果。

## 已完成的修复范围

完整证据和每轮预期/实测见 [五批核对记录](xmind-interaction-review-2026-09-10.md)。该文件按最新批次在前排列；旧批次待办是历史快照，不能直接当作当前未修复清单。

| 文件 | 当前改动与后续关注 |
| --- | --- |
| `src/lib/xmind/scene.ts` | 默认 30/18/14 字号及盒尺寸，思维导图 1–6 分支象限/顺序，真实接点居中，左右逻辑图侧边曲线，上下组织图间距，括号后代继承，左右鱼骨紧凑排列与分侧避让，水平时间轴主轴与上下明细，边界/概要默认外观及测量 |
| `src/components/XmindCanvas.tsx` | 透明主题命中，多选整组拖动、落点提示与 Esc 取消，折叠状态/控件方向及鱼骨斜骨定位，文字草稿、快捷键、搜索/大纲定位、备注/链接、分组及联系交互 |
| `src/lib/xmind/drop.ts` | 新增拖放目标几何逻辑；跨机迁移不能漏掉 |
| `src/components/XmindView.tsx` | 格式面板按主题类型解析样式，画布外撤销/重做与文字输入隔离，概要/批注大纲项、草稿保存桥及相关交互 |
| `src/lib/xmind/document.ts` | 编辑命令、分组/主题迁移、折叠和数据保留等修复；既有未知字段与附件保留约束继续生效 |
| `src/components/xmind.css` / `src/lib/i18n.ts` | 悬停/选中折叠按钮、专用交互样式与中英文文案 |
| `src/lib/fileio.ts` | 原生 Cmd+O 过滤表补 `.xmind`，避免文件置灰 |
| `scripts/test-xmind.ts` / `scripts/test-regression.mjs` | 核心布局/归档测试与组件交互回归；具体断言是验收辅助，不替代原版比对 |
| `tests/xmind-review.tsx` | 独立浏览器验证入口，仅加载生成的样例，不启动完整 App 或读取用户持久化状态 |

常用结构基础样例已比对：思维导图、左右逻辑图、上下组织图、括号图、左右头鱼骨图、水平时间轴。不能因此声称任意模板或自定义样式一致。

## 最近实际验证结果

- `npm run test:xmind`：58 项通过。
- `npm run test:regression`：91 项通过，0 失败。
- `npm run build`：TypeScript/Vite 通过；既有大 chunk 提示保留。
- 独立 macOS `.app` 已构建并重启，用最新二进制实测时间轴长标题、鱼骨斜骨按钮及展开/折叠、括号、上下组织图、左右逻辑图、暗色控件。
- 原生 Cmd+O 可以打开新生成的 `.xmind`。时间轴 Cmd+/ → Cmd+S 后磁盘 `branch=folded`；Cmd+Z → 中文草稿未离焦 Cmd+S 后标题落盘。关闭重开后新标题和展开状态保留。其他七张工作表逐项不变。
- 原版 XMind 打开保存结果 `native-edit.xmind`，无修复提示，显示中文新标题与全部工作表。
- 最终包鼠标点击鱼骨斜骨控件 → 保存，磁盘折叠标记正确；撤销再保存后整个八工作表文档与保存前逐项相等。
- 浏览器 720px 暗色窗口和八工作表切换已检验；页面无横向溢出，工具栏未被裁切。白色画布是文件背景，不能随应用暗色主题改写。
- `npm run perf:xmind` 通过：1,001 主题及 1MB 附件，10 次布局中位数约 70.4ms，10 次归档写回约 8.6ms；另一次 4,001 主题时间轴布局约 218ms。这些是本机计算耗时，不是新机器性能承诺或整帧时间。
- `git diff --check` 通过。第五批源码和原生二进制 SHA-256 在 `tests/artifacts/xmind-round5/verification.json`。恢复后先核对源码哈希；源码变化后旧证据仅作基线。
- 正式 `/Applications/DEditor.app` 没有被替换；用户看到正式安装仍可能是旧实现。不要把测试包已构建说成正式安装已更新。

## 未完成清单与下一步顺序

以下为待验收范围，**不等于都已确认存在 bug**。发现差异后实际修复，验证通过后更新状态和证据路径；没有证据的项保持未验证。

| 顺序 | 待办 | 要取得的验收证据 |
| --- | --- | --- |
| 1 | 建立完整功能/视觉核对矩阵，并检查复杂模板、自定义形状和主题继承 | 先读实际渲染支持，再在原版新建覆盖样例；包括未覆盖形状、显式/继承填充与连线、不同字号、中英长标题、图片/标签/图标组合。记录每个对象的原版表现、当前表现、差异、修复和复测，不只检查默认结构 |
| 2 | 垂直及其他时间轴变体 | 当前 `directionOf()` 仍将时间轴归一化为同类；只有 `org.xmind.ui.timeline.horizontal` 有本批原版校准实现，其他变体仍近似并提示。用原版新建样例确认字段和布局，再实现/修复，不能直接取消提示 |
| 3 | 复杂分组、联系、多选格式和混合结构交叉场景 | 基础边界/概要、联系控制点已有修复；继续核对自定义分组形状、嵌套/交叉分组、关系端点与箭头、混合值面板、多选编辑、拖放后连线与历史。按当前源码确认剩余差异，避免重做已完成项 |
| 4 | 真实输入法与极端缩放/长草稿 | 必须真实组合输入，验证 composition/候选窗口位置、Enter/Esc 与导图快捷键隔离、中文多行、极端缩放；粘贴中文或 setValue 不能作为 IME 测试证据 |
| 5 | Windows 原生验证 | 在 Windows 真机/可用环境验证打开、保存/另存、重开、Ctrl 快捷键、文件对话框、拖放、字体、缩放、输入法及原版互通；macOS 和 jsdom 结果不能代替 |
| 6 | 全量收口与可交付包 | 矩阵未完成项清零或由用户明确调整范围；跑相关 3–5 轮不同用例及完整回归、构建、真实包验证，确认交付源码与证据对应。部署/正式安装状态单独报告，不能用它替代功能验收 |

新机器如果没有原版 XMind 或暂时无法控制桌面，记录具体能力缺口，同时推进源码核对、样例准备、浏览器、归档及回归；不要因此停掉不依赖该能力的工作，更不能把未测项标为通过。

## 新机器启动与复现

1. 读本文件和第五批记录，检查 Git 状态、交接包文件哈希和未跟踪文件。当前目录可能与旧路径不同。
2. 使用项目锁文件安装依赖，准备 Node/npm、Rust stable 与平台构建工具；以仓库锁文件及 Tauri 配置为准，不依赖旧机器的 `node_modules` 或 `target`。
3. 为验证当前恢复基线运行：

```sh
npm ci
npm run test:xmind
npm run test:regression
npm run build
```

4. 浏览器独立入口：

```sh
npm run dev -- --host 127.0.0.1 --port 5173
```

`http://127.0.0.1:5173/tests/xmind-review.html` 默认使用代码生成样例，可独立运行。附加 `?dark` 检查暗色。

迁移证据目录后可查看已有生成探针：`?probe=structure-baseline`、`?probe=layout-baseline`、`?probe=native-edit` 对应第五批；`?probe=interaction-probe` 对应第三批；`control-probe` / `direction-probe` / `count-probe` 对应第二批。缺少被忽略的样例会导致 fetch 失败，应先恢复样例或生成新样例，不要误判为应用解析缺陷。新一轮验证应新建独立目录和输入文件。

5. macOS 独立测试包使用已纳入仓库的配置，替代旧机器 `/tmp/deditor-native-round3.conf.json`：

```sh
npm run tauri -- build --bundles app --config tests/xmind-native-review.conf.json
```

输出：`src-tauri/target/release/bundle/macos/DEditor XMind Review3.app`。identifier 为 `com.deditor.xmindreview3`，清空文件关联。Review3 是沿用的测试应用名，不代表只包含第三批代码。每次源码修改、重新构建后要退出旧进程，再启动新包验证；应用正在运行不意味着它已经加载了新源码。Windows 使用匹配该平台的构建产物重新验证。

## 操作边界与已知验证陷阱

- 仅使用本轮自己构造的测试输入或用户主动提供的文件；不扫描用户真实文档、历史记录、应用持久化状态来寻找样本。历史生成样例只作证据和基线，不代替新一轮实测。
- 前端文件 IO 继续走 Rust invoke；浏览器验证入口只为隔离测试加载已知生成样例，不要为测试给正式前端开自由文件访问。
- 真实 UI 用可用的 CUA/浏览器控制工具操作。不要靠脚本注入应用状态来伪造交互通过。
- macOS CUA 曾偶发截图流失败、AX 过期、窗口切换后截图滞后；刷新状态、重新获取/提升目标窗口并核实截图后继续。一次截图失败不等于锁屏或业务缺陷。对话框确认须等路径实际选中、Open 可用，避免连续按键打到尚未出现的界面。
- 折叠按钮在悬停或选中主题后才显示；先按真实操作使其可见，再点击。隐藏 SVG 控件的程序事件不能替代鼠标实测。
- 原版截图有缩放差异，要归一化框尺寸和缩放比例再比对；不能把缩放差误改成字体/宽高差。
- 生成 `.xmind` 时 topic ID 必须在工作簿内唯一，manifest 要有有效 `file-entries`。之前的重复 ID / 不完整清单导致原版拒绝或要求修复，属于测试样例构造错误。正确样例的互通已经通过。
- 不删除或覆盖既有工作区改动，不默认提交/推送/安装正式包。当前用户要求是保存上下文以换机继续。

## 整体完成的判断

完成意味着核对矩阵中约定范围的 UI/功能项都有实际证据、发现的问题已经修复且复测通过，相关平台和输入法完成原生验证，保存结果与原版互通，最终证据对应最终源码。测试数量只是覆盖情况的统计，不能作为结束条件。仍存在上面的未验收项时，继续工作并明确状态。
