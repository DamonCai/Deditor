# 侧栏入口精简验证

移除 FileTree 顶部双箭头折叠按钮，以及 App 侧栏收起时的 8px 左侧展开栏，清理对应图标、CSS、双语文案及无用订阅。统一使用标题栏侧栏按钮，保留 Cmd/Ctrl+B 和侧栏宽度拖动。

轮 1 — 用例：标题栏点击收起、展开，检查浅色展开和暗色收起布局。
  期望：无旧折叠按钮，收起后编辑区贴左且无窄条占位。
  实测：localhost:5173 浏览器实际截图符合预期；按钮选中状态和路径输入区随显隐正确更新。
  结果：通过。

轮 2 — 用例：标题栏按钮获得焦点后 Enter 展开，再 Cmd+B 收起。
  期望：键盘操作仍控制同一侧栏状态。
  实测：AX 状态依次从 0 到 1 再到 0；路径输入框及选择文件夹按钮在展开时出现。
  结果：通过。

轮 3 — 用例：相邻 UI 回归与 TypeScript/Vite 构建。
  期望：既有回归通过，构建无错误。
  实测：41 passed, 0 failed；清理 App 中不再使用的翻译 hook 后，npm run build 成功（保留原有 chunk 大小警告）。
  结果：通过。

范围：此次仅修改前端，使用浏览器验证，未重新打包或运行 macOS/Windows 安装包。
