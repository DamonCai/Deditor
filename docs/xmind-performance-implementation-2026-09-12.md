# XMind 性能优化实施与验证

后续补验：原生接口恢复后，Review26 已完成下文 1,001 节点/8MiB 文件从磁盘重开，原版 XMind 也已读取并识别 1,001 主题及完整保存标题。无 theme / 根节点无填充的标题显示存在原版差异，未标样式一致通过。详见 [剪贴板修复与补验](xmind-clipboard-fix-2026-09-12.md)，下文超时状态为当时记录，不能沿用为当前永久阻塞。

用户授权“开始优化”后完成四项改动：Base64 使用原生能力或有界 UTF-16 分块回退；颜色/填充/边框颜色命令复用布局，字号、标题、折叠等仍重新计算；不需要换行的标题减少逐字测量；标题编辑只复制祖先路径，画布草稿单独生成编辑节点。保存仍同步生成完整归档快照，未改变自动保存、dirty 或恢复时机。

## 前后测量

`scripts/perf-xmind-editing.ts --baseline=/private/tmp/deditor-xmind-native-round20-review23` 使用相同确定性自建数据，预热一次、五次样本取中位数。Node v23.11.1 / macOS，毫秒。仅计算阶段，不代表端到端按键延迟或原生 IPC/磁盘耗时。

| 场景/阶段 | 优化前 | 优化后 |
| --- | ---: | ---: |
| 8MiB 附件，Base64 | 135.82 | 9.44 |
| 5,001 节点，标题提交文档操作 | 2.87 | 0.08 |
| 5,001 节点，完整布局 | 52.36 | 28.12 |
| 5,001 节点，改色画布更新 | 49.01 | 2.02 |

前后生成的完整 data URL 精确一致；基准含编码 roundtrip、缓存结果与完整布局深比较。证据 `tests/artifacts/xmind-performance-20260912/comparison.json` 含源码哈希。历史结构共享已有引用/不可变回归，但本次未直接测量堆内存，不能给出内存下降比例。

## 自动化与浏览器

固定快照 `/private/tmp/deditor-xmind-performance-review24`：116 XMind 核心、143 组件、46 Markdown 核心、85 阅读集成，以及导出/语法/图标完整回归通过；旧 XMind 性能回归与生产构建通过。编码覆盖所有字节和三字节/分块边界；缓存覆盖全部结构、主题、浮动/概要/批注、撤销和失效；新增 301 节点连续草稿用例验证输入框不被替换、最新草稿保存、撤销/重做和 Escape。原有组件测试有 React createRoot 警告，不能把日志说成完全无警告。

真实浏览器自建 1,001 节点页面：连续逐键输入、提交、撤销、重做、取消草稿、字号重布局、无填充及折叠 1,001→1→1,001 均通过，未捕获页面错误。颜色输入工具 fill 未实际提交颜色值，因此该动作不计为改色 UI 验收；无填充按钮则确实改变形状与自动文字颜色，坐标保持不变。未计作真实中文组词。

## 原生已完成与边界

独立 Review24 包 SHA-256：`695b05ae3edb85b4d6120cc61fa0b21e285a95ea6c90a1f6a15b622afb0874b5`。

1,001 节点、8MiB 随机附件原生样例已打开、就地编辑、未离焦保存。实际标题为 `Native performance optimized ABC23`；工具发送的单键序列与实际大小写/数字不完全一致，不能计作逐键准确性或 IME 通过。保存标题与 UI 显示一致，其他所有 JSON 字段及 ZIP 条目精确保留；原生撤销后保存整份文件与基线逐字节一致。重做、无填充和 40pt 字号已原生保存，精确归档校验通过。

文档已关闭。再次系统打开时按钮置灰，重定位及文件项动作未恢复；改走应用文件树时原生接口连续返回 noWindowsAvailable / timeoutReached，截图也超时。**Review24 这份样例的原生重开、原版 XMind 读取尚未完成**。关闭文档后精确退出本测试进程，进程检查确认 Review24 / 原版 XMind 主进程均不存在；浏览器测试标签也已关闭。

原始完整范围仍有跨文档图片/联系复制缺口、主题形状组合、持续压力验收、真实 IME 和 Windows；本性能批次通过不代表整体 XMind 工作完成。期间工作区被外部操作提交到 `ee2974f`，本执行未运行提交/推送，后续仍需重查 Git。
