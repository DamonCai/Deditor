# XMind 第十八轮：对称阶段折叠与标签显示

整体仍在验收中，本文件不是全量完成声明。仅使用自建样例。

## 原版证据及修复

- Review18 对称时间轴保存的阶段折叠字段被原版忽略，而普通向上时间轴及鱼骨折叠有效。原版对称阶段为放射子图，不提供折叠。现统一命令、场景和控件的可折叠判定；旧字段保持原样，不影响普通明细或独立主题折叠。
- 原版 `/tmp/deditor-xmind-round18-labels/label-overflow.xmind` 在 134% 展示六条长标签为两条单行省略加 `4+`；16 条短标签在三行内全部显示。双击 `4+` 的编辑窗口包含完整六条文字。
- 本地长标签改为单行省略，最多三行，超出后给出剩余数量。13px Helvetica/Arial、白色 70% 底、黑色 10% 细框、黑色 50% 文字、8px 圆角、行/列间隔 2px、主题间隔 4px，与原版已观察规则对应。省略仅用于显示，保存不截短原文。
- 标签双击或键盘 Enter/Space 打开完整标签编辑字段；完整文字也保留在辅助访问名称与提示中。

## 已完成验证

- 核心 108 项：包含旧折叠字段字节保留、普通后代折叠、长标签保存保真、3/4/6/100 标签数量和三行上限，以及 16 结构 × 展开/部分折叠/全部可折叠组合间距。
- 组件 142 项，0 失败；完整 `test:all` 通过。首次运行仅一条旧断言仍期待窄主题完整显示 `visual-QA` 而失败，按新原版语义将该基础控件用例改为短标签，并增加独立长标签/隐藏数/完整编辑/撤销用例。
- 浏览器组织图图片标签无覆盖；真实双击标签读取全文，输入六条长标签显示 `4+`；Enter 打开隐藏标签，一次撤销恢复原三条。上下对称时间轴阶段无折叠入口，快捷键不隐藏主题。
- 原版本轮和恢复的旧自建窗口已逐份关闭，最后退出应用；未关闭用户正在使用的正式 DEditor。

## 证据与待继续

`tests/artifacts/xmind-round18/`：`core.log`、`all-review20.log`、首次旧断言记录 `all-initial-label-expectation.log`、`source-review20.json`、`build-review20.log`。固定源码快照 `/private/tmp/deditor-xmind-native-round18-review20`，新原生包构建及保存/重开验证进行中。

仍需新包原生与原版闭环，以及既有原生整段拖动、真实候选窗口、复杂组合和 Windows 验收；不得计为已通过。


## Review20 原生及原版闭环

构建成功，包 SHA-256 `aa1123805e81b2ebc67e528ae661d559394d1ed6417f17fda5a63609b341a2e5`。原生双击 `4+` 聚焦完整标签字段，第六条追加 ` Native labels saved`，未离焦保存、关闭重开均保留。归档仅该标签变化，其他 JSON 与资源相等。原版准确读取新路径，134% 仍显示两条加 `4+`，双击可见第六条新文字，无修复提示。

上下对称样例含历史阶段折叠字段；新包均显示全部 21 主题，阶段没有折叠入口。选中阶段 Cmd+/、Cmd+S 后字节与输入完全相同且撤销不可用。向上阶段改名 `Native Review20 symmetric up` 后保存/关闭重开，原版 80% 同样显示 21 主题和新标题。归档只有该标题变化，旧 branch 字段未擅自改写。

详细哈希见 `native-review20-verification.json`；两份保存样例也已复制到证据目录。每份验证后关闭文档，最后退出 Review20；原版核验后也逐份关闭并退出。本轮这两类修复闭环完成，整体继续推进第十九轮原生拖动与剩余验收。
