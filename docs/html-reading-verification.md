# HTML 阅读模式验证

实现：`.html` / `.htm` 支持编辑、实时预览、阅读三种视图；读取当前编辑内容；保留页面样式和相对路径资源；iframe 沙箱禁用脚本、嵌套页面及表单提交。视图设置沿用 Markdown 的全局设置。

| 轮次 | 用例 | 期望 | 实测 | 结果 |
| --- | --- | --- | --- | --- |
| 轮 1 | 未保存的 HTML、样式和图片 | 渲染当前内容，保留排版 | 自动化通过；浏览器中本地 CSS、图片正常显示 | 通过 |
| 轮 2 | 空文件、不完整标签、中文、Windows 路径、错误 base | 内容可读，资源地址正确，错误 base 回退 | 自动化通过；补充独立验证错误 base 回退通过 | 通过 |
| 轮 3 | 三模式切换、多标签、编辑后撤销、文内锚点 | 预览跟随内容，保留撤销，锚点留在当前文档 | 自动化通过；浏览器模式切换及锚点跳转正常 | 通过 |
| 轮 4 | 脚本、嵌套 iframe、表单、meta refresh | 阅读文档不能执行上述主动行为 | 沙箱和 CSP 断言通过；浏览器中的测试脚本未执行 | 通过 |
| 轮 5 | 相邻保存、Markdown 预览、编辑器缓存、对话框回归 | 相邻功能保持正常 | 首轮完整回归 30 项通过、0 失败；后续复跑被正在变化的 XMind 文件语法错误阻断 | 首轮通过，最终复跑受阻 |

执行命令：`npm run test:regression`、`npm run build`、独立 HTML 文档构造测试和浏览器 UI 检查。

完整构建未通过：最新构建报告 `src/components/XmindView.tsx:82` 存在两处 `TS1005: '}' expected`。该文件属于工作区内同时进行的其他改动，本次没有修改。最初类型检查也仅报告 XMind 相关错误；HTML 文件未报类型错误。浏览器使用实际 HTML 组件及测试 IPC/资源路径映射，不等同于 macOS/Windows 安装包实测。


## 按钮样式统一复验

HTML 和 Markdown 现在共用 `PreviewModeSwitch`，逐字保留原 Markdown 分段按钮的尺寸、圆角、边框、字体、选中及悬停样式。HTML 工具栏水平留白同步为 8px。

| 轮次 | 用例 | 期望 | 实测 | 结果 |
| --- | --- | --- | --- | --- |
| 轮 1 | 阅读当前 HTML、文件保存主路径 | 渲染和保存正常 | `--round=1`：6 项通过，0 失败 | 通过 |
| 轮 2 | 空内容、不完整 HTML、Unicode 路径等边界 | 正常处理边界输入 | `--round=2`：6 项通过，0 失败 | 通过 |
| 轮 3 | 模式切换、多标签、撤销与异步保存组合 | 模式状态正确且保留编辑记录 | `--round=3`：9 项通过，0 失败 | 通过 |

本轮 `npm run build` 已通过（TypeScript + Vite）；上一轮记录的 XMind 构建阻塞已不再出现。Vite 保留既有大包和混合导入提示。
