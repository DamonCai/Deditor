# Quality 区域逐项视觉复核：不一致，未通过

本轮回应用户要求，从线、连接点、图形、文字及特殊效果分别核对。本轮是差异诊断，没有修改渲染实现，不把前一轮图标修复或单元测试通过当作视觉一致性通过。

## 证据和对象

- 原文件：`tests/artifacts/xmind-native-review/complex-styles-original.xmind`，本次读取 SHA-256：`f350d46fd2fe7df5c79be8977495b31d16f033d1336502e59e0b12e6bbadb339`。
- 用户本轮截图原样保存为 `tests/artifacts/xmind-quality-detail-audit/user-quality-comparison.png`。左为 DEditor、右为 XMind；黑圈是用户标注，不是导图元素。
- 复看已有原生截图 `tests/artifacts/xmind-original-audit/xmind-01.png`、`deditor-01.png`，均为 original 综合样式页。本轮没有重新拍摄应用截图，不把旧图描述为修复后的新图。
- 当前代码场景诊断保存在 `tests/artifacts/xmind-quality-detail-audit/scene-diagnostic.json`。其中宽高由默认文字估算器计算，只用于验证路径分支、颜色和坐标是否生效，不是原生字体像素测量。

## 1. 线

| 编号 | 对象 | XMind 参考表现 | DEditor 实际及原因 | 结论 |
| --- | --- | --- | --- | --- |
| L1 | Quality 下属分支颜色 | 可读性、交互及叶子下划线均沿用紫色 | 主节点 `#9333EA`，子节点计算成 `#DB2777`。父节点明确颜色没有传递到后代，左侧分支反转后索引也参与错配 | 不一致 |
| L2 | Quality 到可读性、交互的路径 | 主节点左侧一段横线接公共竖干，两端圆角转向子节点 | 两条独立三次曲线，各自从主节点中点出发，没有参考图中的公共圆角竖干 | 不一致 |
| L3 | 圆角折线样式识别 | 文件主主题样式为 `org.xmind.branchConnection.roundedElbow` | `edgePath` 用区分大小写的 `includes("elbow")`，未匹配 `Elbow`，回退普通曲线；诊断输出为 `C` 路径 | 不一致，已定位 |
| L4 | 更深层的分支 | 子主题的下划线与圆角连接线连成整体 | 当前子主题未继承相应连接方式，继续生成普通曲线 | 不一致 |
| L5 | 线宽、圆角半径、主干弧度 | 需统一缩放与定位后测量 | 两边截图缩放不同；不能直接用屏幕像素得出精确差值 | 待同尺度测量 |

## 2. 点与端点

| 编号 | 对象 | XMind 参考表现 | DEditor 实际及原因 | 结论 |
| --- | --- | --- | --- | --- |
| P1 | 下划线节点接入点 | 连接落在标题下方的下划线上 | 普通横向 `edgePath` 使用节点矩形的 `cy`，接到矩形侧边中间，与下划线端点不重合 | 不一致 |
| P2 | 展开/折叠圆点 | 用户截图静态画面中未显示这些圆圈 | DEditor 对所有有 attached 子节点的主题持续绘制圆圈和减号 | 截图状态不一致；XMind 悬停/选中显示规则仍需交互实测 |
| P3 | 左侧节点折叠按钮位置 | 不能从无按钮的参考图确认 | DEditor 一律放在节点右侧 `width + 11`，未区分左右展开方向 | 实现已确认，正确目标位置待交互实测 |
| P4 | 主节点选中外框 | 参考区域未提供相同选择状态 | 左图蓝色外框属于选择状态，不能直接判定为文档边框渲染错误 | 排除状态差异后再测 |

## 3. 图形

| 编号 | 对象 | XMind 参考表现 | DEditor 实际及原因 | 结论 |
| --- | --- | --- | --- | --- |
| S1 | 批注外形 | 粉色圆角气泡与填充的细长尖尾 | 普通圆角矩形加正交折线，`children.callout` 最后调用普通 `add(f, "up")` | 明确不一致 |
| S2 | 批注边框 | 截图中没有左图那样的明显粉色描边 | NodeShape 为批注套用普通节点默认边框，计算描边为 `#DB2777` | 不一致 |
| S3 | 主节点圆角矩形 | 形状类别和紫色填充相近 | 同为圆角矩形，但精确圆角、内边距、字号未对齐测量 | 仅类别相同，不能验收为相同 |
| S4 | 标签外形 | visual-QA、跨平台各自是浅灰圆角胶囊 | 合并成普通辅助文字，没有胶囊外框 | 不一致 |

## 4. 文字、标签和图标

| 编号 | 对象 | XMind 参考表现 | DEditor 实际及原因 | 结论 |
| --- | --- | --- | --- | --- |
| T1 | 标签分组与位置 | 两个标签分别放在标题下方，截图中分两行 | 当前 `labels.join(" · ")` 再自动折行，丢失单个标签的视觉边界 | 不一致 |
| T2 | 备注标记 | 标题“备注与标签”右侧的小横线图标 | 前一轮已将“备注”等辅助文字换成图标，但当前代码仍把图标放在标签后面的独立行 | 仍不一致，不能把“有图标”算作“图标一致” |
| T3 | 标题内容 | Quality、可读性、交互等内容对应 | 内容基本对应；内容相同不代表几何、字体及辅助元素相同 | 内容对应 |
| T4 | 字体、基线、字重、换行、文字越界 | 应在同尺度下逐节点看四边留白和字形 | 用户当前截取中长标题被画面边缘截断，不足以单独判定节点溢出；早先菱形溢出问题不能据此宣布全部解决 | 待局部同尺度测量 |
| T5 | 无用名字 | marker ID 和结构类名不是用户可读标签 | 前一轮替换了这些内部名；原文件真实 labels 必须保留，只修表现，不能全局删除类似名称 | 内部名修复与标签样式验收分开 |

## 5. 批注等特殊效果

| 编号 | 对象 | XMind 参考表现 | DEditor 实际及原因 | 结论 |
| --- | --- | --- | --- | --- |
| E1 | 批注位置 | 位于 Quality 上方，尖尾落向主节点 | 原文件保存 position `{x:0,y:-125.359375}`，当前代码用固定偏移把批注放到右上方，没有读取该 position | 不一致 |
| E2 | 位置字段生效 | 改变批注坐标应影响显示位置；具体坐标语义还需原生实测 | 只在内存把坐标改为 `{x:300,y:-500}`，再次 buildScene 后批注 x、y 完全相同；原文件未写入 | 忽略字段已确认 |
| E3 | 气泡尾巴 | 有填充、有宽度，逐渐收窄到尖端，颜色与气泡一致 | 当前是 `fill=none` 的折线，不具备气泡尾巴的面积与轮廓 | 不一致 |
| E4 | 阴影、透明度、渐变 | 当前局部参考不足以可靠区分阴影与截图背景 | 不把“没看出来”记成“已一致” | 待验证 |

## 分轮核查记录

1. 截图核查：预期同类元素逐一对应；实际看到连线、公共竖干、气泡、标签和按钮显示差异。视觉一致性不通过。
2. 原文件字段核查：预期找到批注、样式和真实标签；实际确认 callout 类型、position、roundedElbow、紫色主题样式与两个 labels。数据读取通过，且说明问题发生在呈现阶段。
3. 当前布局执行核查：预期还原代码实际输出；实际得到紫色主节点/粉色后代、三次曲线、普通批注折线；内存坐标探针确认 position 被忽略。缺陷复现通过，视觉一致性不通过。

本轮没有修改业务实现，因此没有以构建或回归测试替代上述视觉核查。

## 后续验收顺序

先完成同一 Quality 区域的连线颜色和几何、下划线端点，再处理批注气泡与坐标、标签胶囊与行内图标，随后验证折叠控件在左右展开、悬停、选中和折叠状态下的表现。每个改动保留同文件、同页、同主题、同交互状态的修复前后局部证据。

这一区域通过后再核对综合样式中的关系虚线、箭头、边界标题、概要括号、图片比例、椭圆和菱形，以及自由主题方向；然后逐页处理组织结构、时间轴和原文件鱼骨。尚未取得可靠原生对照的条目保持“待验证”。禁止用 verified 文件、只看总览、仅凭无溢出或单测通过给 original 全部布局验收通过。
