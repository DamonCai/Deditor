# 本地预览限制检查与修复（2026-09-20）

用户指出本地编辑器不应禁止正常 HTML 行为，并要求检查类似问题。本次接续 `16efb34`，保留上一轮未提交的验证入口和记录；没有修改用户提供的 HTML 文件，没有执行提交或推送。

## 检查结果与处理

| 位置 | 原问题 | 本次处理 |
| --- | --- | --- |
| 独立 HTML | 上轮虽然允许了脚本，额外 CSP 仍禁止 eval、内嵌页面、表单等；刷新标签被删除 | 移除编辑器追加的 CSP 和刷新删除逻辑，保留文件作者自己的 CSP。sandbox 开放脚本、表单、弹窗、下载和模态对话框，继续隔离编辑器主页面 |
| Markdown HTML 围栏和块 | 脚本、事件属性被删除，依赖它们的内容变成空白或无法交互 | 有行为或页面级样式的 HTML 使用独立 srcdoc 文档；静态 SVG 和普通片段维持原来的文档内显示。HTML 围栏补明确的编辑源码按钮 |
| Markdown iframe | 非 HTTP/HTTPS 地址直接被删除，srcdoc 内容被删除，全屏属性丢失 | 保留相对路径、file 地址和 srcdoc；以隔离文档挂载，保留作者的全屏属性。srcdoc 在主清理器外保存为惰性文档数据，避免内部脚本再次被删除 |
| 本地资源 | file 图片在清理时丢失；音视频、poster、字幕、source、SVG image/use 未转换路径 | 按文档路径转换 asset 地址，保留时间/片段后缀；保留 SVG use。已挂载 source 发生路径变化时重新调用播放器 load，同路径重复处理不重置播放 |
| 阅读编辑交互 | 点击音视频或按 Enter 被外层当成编辑源码 | 媒体及表单控件保留自己的鼠标/键盘操作，编辑使用明确入口 |

同时检查了独立图片/PDF/音视频预览、Mermaid/PlantUML、Markdown 链接和导出路径：独立媒体没有类似的脚本禁用策略；Mermaid 已用 loose 模式。Office/PDF 导出的静态处理不属于交互式预览，本次没有把它改成执行页面脚本。未改原生能力授权、日常安装包或远程服务配置。

仍保留预览与主编辑器的不同源隔离，文件脚本不能读写编辑器 DOM、存储或原生桥。该预览不是完整浏览器环境；依赖同源存储/跨域授权的页面仍遵循 WebView 规则。没有移除文件自己声明的 CSP，也不能覆盖网站自身的禁止嵌入策略。

## 五轮验证

仅使用自建样例。浏览器入口 `tests/preview-restrictions-review.html`，资源位于 `tests/fixtures/preview-restrictions/`；1 秒静音 WAV 和 160×90 H.264 视频自行生成于被忽略的 `tests/artifacts/preview-restrictions-2026-09-20/`。

1. **基础行为**：期望脚本、事件、计算和内嵌页正常工作。实际独立 HTML 与 Markdown 均计算出 42，按钮更新成功，本地表单提交事件显示输入，内嵌页面可见，外链 helper.js 执行成功。通过。
2. **路径与边界**：期望 file、相对路径、Unicode/Windows、空文件路径和片段不丢。自动化覆盖这些路径、作者 CSP/refresh 保留及重复加载；浏览器本地图片自然宽度 80px、音频 readyState=4/时长1秒，本地 iframe 按钮与 srcdoc 脚本实际执行。通过。Windows 仅路径单测，未做 Windows 原生验收。
3. **交互组合**：期望阅读编辑与实时预览一致，能编辑和撤销。实际源码入口修改动态 HTML 后立即更新，单次撤销恢复完整原文且 dirty 为空；720px 英文暗色实时预览正常，静态 SVG 保留，原生 HTML 块脚本正常执行。通过。
4. **媒体与隔离**：期望播放器可操作、不误进源码，脚本不影响主编辑器。自建音频实际播放；发现 source 改址后不加载，修复后视频出现160×90播放画面和暂停/进度控件；操作后源码编辑器数为0、dirty为空。浏览器子页面访问父 DOM 被拒；自动化保留无同源授权、srcdoc 和 script 不进入主 DOM。通过。工具读取 video 元数据连续超时，因此未将这部分属性读取计作通过；视频结论来自实际播放画面和控件。
5. **回归与构建**：完整 `test:all` 退出0（包含147通用、8分隔线、119 XMind及所有 Markdown 组）。最后 SVG/source 加载修正后额外跑4组预览限制、2组预览块复用、8组特殊块和生产构建，均通过；代码显示32用例通过，`git diff --check` 通过。静态 SVG 的 use 方块和 file 图片另经真实浏览器画面确认，撤销后原文一致。

初次检查让旧的“必须删除脚本/本地 iframe”断言失败，已改成验证独立文档保留内容、主页面仍隔离。初次集成另外揭示 DOMPurify 连 srcdoc 内的脚本文字也会删除，已改为清理前保存、清理后作为独立文档恢复，并新增真实浏览器脚本回归。没有通过忽略失败断言解决问题。

## 平台边界

本次没有重打原生包或替换日常应用。上一轮 macOS 控制工具错误/超时未验收，不能把本次浏览器结果记为 macOS/Windows 安装包通过。外部脚本以本地 helper.js 验证，不涉及远程 CDN；本地 SVG 资源的原生跨协议加载亦未新增验证。

最终临时浏览器页面和本次 Vite 服务已关闭；自建样例验证结束时 dirty 为空。用户随后要求将“本地编辑器不要过度限制”写入项目上下文，已补入 AGENTS.md 协作偏好，并将本次修复、测试与记录一并提交；不推送远程。
