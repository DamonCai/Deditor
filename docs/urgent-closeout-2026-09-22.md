# 紧急并行收尾（2026-09-22）

用户在周额度剩 37% 时要求估算并开多个 agent 推进紧急项。本轮采用三 agent 处理配对、链接卡片、混合文档/多窗口，主端整合与独占原生桌面。起点为干净提交 `735e731`。

## 本轮完成

**关闭两类待办：链接悬浮卡片、混合文档及多窗口组合。** 另修复确定的 DOM 合并输入配对缺口；此前首次原生额外星号的事件未捕获，仍保留核查。待办从 10 类降至 **8 类**，不把真实 IME 或 Windows 等未验范围计入完成。

| 范围 | 结果 |
| --- | --- |
| 配对输入 | 已观察到的多个普通文本事件被 DOM observer 合并时，仍正确处理生成闭合符；粘贴/composition/未知事件保持字面输入。内部事件缓存超限整批回退，完整正文不截断。17 组专项及普通原生包快速输入保存重开通过。见[配对记录](urgent-format-pairs-2026-09-22.md) |
| 链接卡片 | 真实拖选触发悬浮入口，确认/取消/删除、原反向选区恢复、直接续写、保存与分步撤销/重开通过。修复更改 URL 丢标题以及合法 file URL 被清空；新增原生亮暗标题保留、本地地址保存/打开/撤销/重做/重开证据。见[链接记录](urgent-link-card-2026-09-22.md) |
| 混合文档 | 同一原生窗口 Markdown/XMind 交替保存、独立撤销/重做，逐阶段核对 Markdown 原字节、XMind content.json 全结构和其余 ZIP entry 原字节；保存重开恢复三节点。见[混合验证](urgent-mixed-windows-2026-09-22.md) |
| 多窗口 | 第二窗口新开文件广播到第一窗口近期列表；新文件就绪后及已有标签选择后，直接输入、保存与撤销源码精确。上游默认选中条目出现在 AX Selected，未误判为丢记录。见同上记录 |

## 验证与构建

- 完整配对专项 17 组、新链接卡片 9 组、输入/剪贴板 23 组、阅读集成 107 项在最终整合中通过；随后仅增加 reference 改 URL 的第 10 组链接断言，单独通过，产品源码未再变化。
- 另有原链接 6 组、近期文件 5 组通过；配对事件容量及链接确认/取消并发边界经过交叉审查。类型检查、生产构建与隔离 macOS debug bundle 均退出 0。
- 本轮只改两个产品模块，执行相关整合回归，没有重复完整 `test:all` 或 `perf:all`；上一轮全套结果不冒充本轮重跑。
- 普通原生基线包用于无标题链接完整流程及混合/多窗口；重建最终普通包用于本轮标题/file/配对修复。没有把旧包结果套给新增修复，没有使用追踪注入包替代最终包。

最终测试包：`src-tauri/target/debug/bundle/macos/DEditor Four Items Review.app`，标识 `com.deditor.fouritems-review20260922`。构建配置沿用独立测试标识，不覆盖日常安装包。所有原生文档均为自建样例；结束时关闭文档、退出测试窗口与应用。

日志在被忽略的 `tests/artifacts/urgent-closeout-2026-09-22/`：`build.log`、`native-build.log`、`regression.log`；链接专项日志在 `tests/artifacts/urgent-link-card-2026-09-22/`。混合样例生成和精确校验入口 `scripts/urgent-mixed-history.py` 已随 Git 保存；真实卡片浏览器复测入口 `tests/urgent-link-card-review.html` 也随 Git 保存，未将该页脚本模拟计作本轮原生证据。

## 明确保留

此前原生批量输入首次额外星号未捕获异常事件，本次确定性 DOM 反例只能证明并修复该具体路径，不能断言两者同因。原生快速输入成功也不证明一定走过事件合并路径；仍保留原生核查与真实中文候选验收。

近期文件在阅读编辑器初始化期间尚不可输入；一次未等就绪直接发送字符未进入正文，不算就绪后的焦点失败，也不声称支持初始化输入排队。本轮无外观改版，不新增中文/窄窗样式、Windows、系统拼写红线/菜单、长期内存、外部服务或原版 XMind 全主题结论。其他未完成范围仍逐项列在[当前待办](pending-tasks.md)。

按既有授权提交本地，不推送。
