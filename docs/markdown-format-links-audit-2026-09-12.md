# 阅读编辑格式与链接专项（2026-09-12）

范围：操作清单 D01–D05、G01–G04。只用自建样例；无提交推送，无原生应用操作。

## 本次修复

1. 空光标开启行内代码，输入 `XY`，再次点击行内代码关闭，然后输入 `Z`：旧实现保存成 `` `XYZ` ``，修复后为 `` `XY`Z ``。行内源码关闭时要保留非 inclusive 格式，否则末尾光标被当作格式外，关闭按钮反而再次开启。
2. 行内源码展开后，粗体/斜体/行内代码工具栏丢失选中状态。现在只解析当前行内片段，将源码位置映射回格式位置。不会解析整篇长文。

## 当前证据

| 编号 | 本次已执行范围 | 结果 |
| --- | --- | --- |
| D01 | 工具栏正文和 H1–H6 全部往返，保存、撤销、重做、重挂载 | 自动化通过；绝对 `# ` 输入规则属于原有核心覆盖 |
| D02 | 粗体、斜体、删除线、行内代码选区设置/取消；空光标输入后关闭 | 自动化通过；固定浏览器真实选区加粗/取消和代码 XY→关闭→Z 通过 |
| D03 | 下划线、上标、下标设置/取消；文字颜色替换、背景叠加 | 自动化通过；颜色清除及复杂混合选区仍需专项，不将其概括为全完成 |
| D04 | Esc 收起；Enter 拆格式正文；Tab；相邻正文保真 | 自动化通过；Enter 会重新展开新段中的格式，不能要求结束后永远无展开节点 |
| D05 | 高亮/上下标/emoji，代码和转义字面量；展开修改高亮再撤销 | 自动化与浏览器呈现通过；连续语法键入、候选交错未全面验收 |
| G01 | 选中标签插入链接、保存撤销；目标文档改变后拒绝旧弹窗应用 | 自动化通过；真实工具栏链接弹窗插入通过；引用链接改写/取消链接的完整组合仍待补 |
| G02 | 普通点击不跳转，Cmd 点击锚点跳转，错误锚点不崩溃 | 自动化通过；系统打开外链/本地文件不属于本次新证据 |
| G03 | 富文本脚注浮层 Tab 进入、Esc 关闭返回引用焦点 | 自动化及浏览器键盘通过；点击引用本身会转到正文，不能把之后的 Tab 当作浮层 Tab |
| G04 | 重复引用显示，脚注正文编辑，删除一个引用，保存/撤销/重做 | 自动化通过；全文 Typora 布局对跑未执行 |

独立脚本 `node scripts/test-markdown-format-links-audit.mjs`：10 组通过。日志：`tests/artifacts/deditor-format-audit.log`；去掉本次改动后的失败对照：`tests/artifacts/deditor-format-before.log`。

浏览器使用隔离快照 `/var/folders/ls/ffs5872n5pz2yk47_m92ylvc0000gn/T/deditor-format-audit-naee08j4`，端口 5197。此端口的同一快照去掉两处补丁后真实按键复现 `` `XYZ` ``，恢复补丁并重载后得到 `` `XY`Z ``；最终快照按钮内容与源码一致。共享 5195 和早期有端口协调歧义的 5198 不作为最终证据。

本次脚本早期错误断言把 Enter 正常拆分 `bold` 为两段误判失败，直接打印 DOM 对象又导致进程 137；改为检查编辑后的语义内容与结构，最终 10 组完整运行正常。此记录不代表产品存在死循环。

原生 macOS、真实中文输入法、Windows 及所有复杂组合不计为本次通过。最终整合回归由主任务执行。

## 代码身份

- `src/lib/markdownVisual/commands.ts` SHA-256 `4a6aeaaa8e84d7e7c904d2c79a916128430fc41310795a5fd02ff4facaa1f088`
- `src/lib/markdownVisual/inlineSource.ts` SHA-256 `b2d1a8c92f61fa4e208328cf5b4b4b069a419dc3821ca784a936410f1c2f492d`
- `scripts/test-markdown-format-links-audit.mjs` SHA-256 `79f87e90359faba22a2135b762162bc41ed3bdfc6966a4f3363ebc6df9a57d50`
