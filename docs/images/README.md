# README 效果图

截图使用真实 `EditorGroups`、Markdown、CodeMirror 和 XMind 组件，由 `tests/readme-showcase.tsx` 提供自建内容。没有加载应用持久化状态或用户文档；浏览器截图不代表原生平台验收。

在仓库根目录运行 `npm run dev`，依次打开下列页面，在内容及图表加载完成后截取页面：

| 图片 | 页面 |
| --- | --- |
| `markdown-reading.jpg` | `/tests/readme-showcase.html` |
| `markdown-diagram.jpg` | `/tests/readme-showcase.html?scene=diagram` |
| `split-dark.jpg` | `/tests/readme-showcase.html?scene=split` |
| `xmind.jpg` | `/tests/readme-showcase.html?scene=map` |

本批于 2026-09-19 截取，页面视口为 1280 × 720，未修改产品样式。截图作为文档资源随仓库保存。
