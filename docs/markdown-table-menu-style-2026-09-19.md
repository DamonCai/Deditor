# 表格右键菜单外观（2026-09-19）

用户要求替换截图中大字居中的三项表格菜单。本次仅调整该菜单，保留选中整行、删除行、删除列的原行为。

菜单原先使用原生创建的按钮，只设置 `deditor-btn` 和尺寸数据属性，没有 React Button 提供的内联尺寸，因此文字、行高与布局没有形成完整的控件样式。

现在菜单使用独立的应用字体，13px 字号、28px 行高、左对齐、168px 最小宽度；增加 16px 行列图标，并在选择和删除之间添加细分隔线。沿用应用边框、圆角、主题、悬停/按下和浮层阴影；键盘焦点轮廓放在菜单项内。图标对辅助功能隐藏，菜单项名称保持中英文原文，分隔线使用 separator 角色，不进入编辑器正文。

轮 1 — 用例：真实浏览器中文亮色菜单。
  期望：紧凑、左对齐，文字和图标垂直居中。
  实测：菜单 168×103px，三项均为 28px 高、13px 字号；图标与按钮中心差均为 0px。已检查截图。
  结果：通过。

轮 2 — 用例：英文暗色、放大正文字号、720px 视口。
  期望：菜单不随正文放大，英文不截断，主题颜色与边界正常。
  实测：正文实测 30px 时菜单仍为 13px、168×103px；英文三项完整可见。设置 720px 视口后键盘菜单可正常操作，验证后清除视口覆盖。
  结果：通过。

轮 3 — 用例：Shift+F10、方向键、Enter、Escape、Tab、删除撤销、只读。
  期望：焦点可见，能执行原动作并返回编辑器，只读不出现编辑菜单。
  实测：方向键聚焦 Delete row，focus-visible 轮廓存在；Enter 删除目标行，撤销后全文精确相同；Escape/Tab 关闭菜单；只读右键自定义菜单数量为 0。
  结果：通过。

轮 4 — 用例：已有表格行为、通用回归和构建。
  期望：菜单 DOM 增加图标与分隔线不影响原操作。
  实测：28 表格、145 通用和 8 分隔线通过；生产构建通过。
  结果：通过。

仅用自建样例，没有修改用户文件。日志及验证页在被忽略的 `tests/artifacts/markdown-table-menu-2026-09-19/`。浏览器页与服务已关闭；本次没有重新打 macOS 包或替换日常安装包，没有新增 Windows 原生验证。保留此前未提交改动，未提交推送。
