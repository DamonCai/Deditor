# XMind 第十六轮验证（2026-09-12，进行中）

整体仍未完成。本轮接续第十五轮，继续向上/对称垂直时间轴；真实候选、原生整段拖动及 Windows 缺口继续保留。

## 向上穿轴

原版枚举字段为 org.xmind.ui.timeline.through.vertical.btt。使用本轮自建、独立 ID 的五阶段样例，原版准确路径 vertical-up.xmind / isDirty=false / 81% 整图已确认：中心在最下方，阶段 1→5 依次向上；奇数阶段明细在右、偶数在左；各阶段内部 A 仍在 B 上方。不能用整体镜像反转内部阅读顺序。

本地原先近似分支将主轴横排，已在实际浏览器确认；现采用向上的主轴排布，并独立保持明细栈顺序、碰撞避让和向上端点。新增中英文结构入口。浏览器适应到 60% 后正确显示同样方向。核心回归已加入长中文标题、嵌套边界、主轴边界、不同折叠状态、范围手柄反向判断、归档字段保存；core-upward.log 共 103 项通过。组件范围拖动已扩展，但修改后的完整回归及原生包仍待下一步，不沿用 Review17 的通过记录。

自建样例入口 tests/fixtures/xmind-timeline-variants.ts、scripts/create-xmind-timeline-variants.ts。三个单表文件分别覆盖向上穿轴、向下对称、向上对称；原版对称变体随后继续核对，过程见下文。

向上穿轴的固定测试快照完整 test:all 已通过（103 核心 / 140 组件，all-upward.log），尚未构建对应原生包。原版 symmetric-down 两明细样例已读取，82% 整图所有阶段明细都在右侧；不能由两个明细推断任意数量规则，已追加 --vary-details 的 1–5 明细样例继续核对。

## 对称垂直与向上对称

原版源码注册表把这两种时间轴的直接阶段指定为 org.xmind.ui.map.unbalanced.symmetric。数量递增样例的 81% 全图进一步确认：1/2/3/4/5 个明细分别右 1/2/2/2/3 个、左 0/0/1/2/2 个，左侧按反向顺序排列；每侧围绕阶段中心对称，单明细与阶段同高。向上变体保留同样的左右分配和内部顺序，只将主轴向上生长。两个准确来源路径为 /private/tmp/xm16-count-down/count-symmetric-down.xmind 与 /private/tmp/xm16-count-up/count-symmetric-up.xmind，均 isDirty=false、无修复提示；原文件与数量样例归档保存在本轮证据目录。

本地原先将所有非根节点的 side 方向强制变为单侧；现仅对对称子图保留两侧布局，并去掉普通思维导图的上下偏移，使单侧明细围绕所属阶段居中。临时结构仅参与布局，不写入源主题；新增三个中英文入口。两个方向的真实浏览器数量样例均已对照，59% 全图方向、左右分配和顺序正确。

回归加入原版数量表、源字段保留、长标题、混合组织图、嵌套边界、不同折叠状态；既有范围拖动用例扩展至四种垂直方向。最终 Review18 固定快照完整 test:all 已通过（104 核心 / 140 组件，all-review18.log），原生包正在构建。后续原生保存/重开/原版互通仍需执行，不能以浏览器或构建替代。
