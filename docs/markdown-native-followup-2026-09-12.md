# Markdown 原生补充验收（2026-09-12）

用户追问“能搞完的都搞完了”，本次重新检查实际环境，没有沿用此前锁屏作为永久阻塞。原生按钮可以操作，截图最初报 ScreenCaptureKit -3811，稍后恢复。此前把共享展示自动化完成当作停止点，仍有可推进的本机验收，现补充如下。

## 使用版本与范围

- 由独立 Markdown 发布快照重新构建最新共享展示代码，macOS 构建退出 0，Rust release 约 1m08s。
- 应用 `DEditor Markdown Extended Review`，identifier `com.deditor.markdownextended20260912`；未操作正式 DEditor 或独立 XMind 实例。
- 新样例 `/tmp/deditor-native-final-review/review.md` 来自完整自建复杂 Markdown，图片改为同目录自建 SVG；303 行、5,537 字符。
- 新包二进制 SHA-256：`fac93d8a6a1e84bdcee45c32da36bdbbe47d6a3d2a348b1d70883f40397109c7`。

## 本次补齐

1. **系统目录选择器**：通过原生对话框选中 `external 图片 #目录`，设置实际回填规范化绝对路径。路径框粘贴一度超时，改用可见路径输入框及原生条目动作完成，没有脚本绕过系统对话框。
2. **外部目录复制与保存**：点击整理本地图片，显示复制 2、跳过 0、失败 0；两个生成 SVG 与各自原图逐字节相同。保存后的 Markdown 仅替换两个图片引用，原文其余字节完全相同；中文、空格、井号正确编码为显式 file URL，图片 alt、title 和 width 保留。
3. **关闭设置后的键盘撤销**：点击完成后，Cmd+S 保存，Cmd+Z 撤销图片引用变更，再 Cmd+S；磁盘整篇内容与原始样例逐字节一致。补齐此前仅浏览器验证的焦点恢复场景。
4. **脚注原生键盘入口**：只读模式下 Tab 到脚注引用后出现脚注浮层；下一次 Tab 将可见焦点移到“转到脚注”按钮，Esc 关闭浮层。原生截图可见按钮焦点边框，AX 状态确认浮层移除。阅读编辑中的 Tab 会插入缩进，本次该探测插入已撤销，未把它误计为脚注导航。
5. **保存、关闭、重开**：全部测试输入撤销并保存，Cmd+W 后 Cmd+Shift+T 重新打开同一自建文档，5537 字符及文末内容正确；最终磁盘原文仍完全一致。

## 当前真实阻塞

- **macOS 真实拼音**：`ctrl+space` 后逐键 `ni` 加空格仍得到拉丁文本；`ctrl+alt+space` 后逐键 `zhong` 也没有候选框，确认后没有转为中文。`TextInputMenuAgent` 两次连接超时（截图恢复后也重试），备用 `SystemUIServer` 连接超时。没有通过其他脚本操控系统输入源，也没有将普通按键、AX 文字或直接中文插入算作 composition 通过。要验收原始中文组词跳动，仍需可可靠切换的真实输入源；本次未获得新 composition 证据。
- **Windows**：当前没有可用 Windows 原生验收环境；已有路径与单元测试不能替代 WebView2/微软拼音。
- **真实图床**：没有使用真实配置账号。此前本机协议模拟与原生图片下载/上传证据仍有效，但不等于真实服务验收。

## 清理与接续

外部图片目录设置恢复为 `assets`；两份自建测试文档保存后关闭。检查隔离状态文件仅剩默认未命名页，随后退出测试应用，进程检查确认已退出。原图、原文和独立 XMind 工作区未改动。没有提交或推送。

构建日志、原文/保存结果/图片与机器可读结果归档到被忽略的 `tests/artifacts/markdown-shared-native-2026-09-12/`，跨机需额外迁移。本记录将原生目录选择、设置完成后撤销、脚注键盘入口及保存重开从“待验收”更新为本次覆盖范围已通过；不将其余真实输入法和平台限制改成通过。
