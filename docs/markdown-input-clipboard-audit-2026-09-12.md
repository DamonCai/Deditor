# 阅读编辑 B/C 输入与剪贴板验收（2026-09-12）

按操作清单 B01–B07、C01–C05 检查。全部使用自建文档；本子任务不操作系统输入源和原生应用。并行开发期间共享开发服务曾触发热更新，剪切后样例重置；该次链路已作废，改用固定源码副本重测，不能把开发热更新当产品故障。

## 修复与失败对照

**B06：Esc 取消表情后，同长度的新前缀不再出现候选。** 浏览器和独立集成都复现：输入 ` :sm` → Esc → Shift+Left 两次选中 `sm` → 替换为 `th`。旧代码只记录候选起止位置，虽然内容改变，仍认作已取消查询；页面没有 `:thailand:` 等候选。

`inputAssist.ts` 现在同时记录取消时的文档，文本发生改变即解除该次取消。只有选区更新时仍保持关闭；改成同长度前缀、删改后重输旧前缀会重新查询。浏览器修复后 `:th` 候选重新出现，Down+Tab 选中 `:thermometer:`，正文显示温度计，源码及核对快照均保留正确简写。没有改括号、列表或剪贴板产品逻辑。

## 本次证据与剩余边界

| 编号 | 本次实测 | 结论和边界 |
| --- | --- | --- |
| B01 | 集成以中文、家庭 emoji、组合重音、扩展汉字替换同块选区；保存、撤销、重做、重新挂载 | 这些组合通过；直接输入中文不代表真实组词 |
| B02 | 集成正文行首 Backspace、前段末尾 Delete 合段；浏览器删除扩展汉字、家庭 emoji 及撤销 | 通过；组合重音按宿主行为先删重音、再删基字，没有断裂代理项 |
| B03 | 浏览器尝试明确 Meta+Backspace，本控制环境未执行整行删除 | 本次不计原生通过；已有历史 macOS 证据由最终原生整合复测确认 |
| B04 | 正文首、中、尾 Enter 分段，Shift+Enter 软换行，精确历史与保存 | 集成通过；列表由列表专项负责 |
| B05 | 五种括号/引号自动配对和跳过闭合符；12 类符号选区包裹；关闭偏好；单词中的撇号和前方已有单词 | 集成通过；Markdown 符号选区包裹是原字符包裹，不把转义字符误当语义格式操作 |
| B06 | 方向键选择/Tab确认；Esc取消、同长度替换、选区变化、旧前缀重输；浏览器修复前后对照 | 新故障修复并通过；真实 IME 交错仍未验收 |
| B07 | 本任务没有操作原生输入源 | 未验证；既有模拟不能替代真实候选确认/取消 |
| C01 | 浏览器选中 `beta` → 剪切 → 剪贴板实读 `beta` → 原位粘回 → 键盘撤销/重做；集成跨段选区粘贴替换 | 通过；跨应用/跨窗口剪贴板仍需原生专项 |
| C02 | 首尾换行、行中 tab、CR/Unicode 文本粘贴，保存及精确历史恢复 | 集成通过；普通段落粘贴使用富文本段落语义，连续空行按上游合并，不宣称原字符换行数保留。行首 tab 可作为 Markdown 缩进代码解析，未擅自改变该语义 |
| C03 | 空段粘贴 Markdown 粗体、标题、列表及 HTML 粗体，撤销/重做/保存 | 集成通过；来源应用矩阵与明确的“粘贴为纯文本”产品语义仍需单列 |
| C04 | 粗体、行内代码、链接的源码展开内粘贴含首尾空格及连续空行的文本，收起、保存、历史恢复 | 这三类通过；代码块/保留块粘贴由特殊块专项补验 |
| C05 | 本次未做图片系统剪贴板、文本拖动和原生文件拖入 | 未验证；图片入口转入 H 专项，不能算此项整体通过 |

## 执行与快照

- 独立脚本：`node scripts/test-markdown-input-clipboard-audit.mjs`，**11 组通过**，运行日志 `tests/artifacts/input-clipboard-audit.log`。测试使用隔离 IO、真实编辑组件与事件；文件保存用模拟写入回调核对，不称为原生磁盘闭环。
- 浏览器固定副本：`/var/folders/ls/ffs5872n5pz2yk47_m92ylvc0000gn/T/deditor-input-clipboard-_ol153fu`。早期使用 5198，协调后改为 5199。副本中 `inputAssist.ts` SHA-256：`ec01cb6d1ad8242e0d12dfce4ce4b111c9f195c26606b6773b1df8ff1b80cb82`。快照清单在 `tests/artifacts/input-clipboard-snapshot.json`。
- 本次新增测试与记录，产品只修改 `inputAssist.ts`；未改公共编辑器组件、主集成脚本、原生后端或其他 agent 的文件。未提交推送。完整整合回归由主任务统一执行。
